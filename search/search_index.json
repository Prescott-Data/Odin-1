{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Overview","text":"<p>10K\u20135M Entities per graph</p> <p>300\u2013800ms Typical retrieval</p> <p>3-signal Path scoring</p> <p>MIT Open source</p>"},{"location":"#graph-intelligence-for-autonomous-ai-agents","title":"Graph intelligence for autonomous AI agents","text":"<p>Odin is a Python library that guides AI agents through large knowledge graphs. It combines Personalized PageRank, learned edge-plausibility scoring (NPLL), and pattern detection to surface high-signal paths \u2014 so agents spend their compute on what matters.</p> <p>Get started Concepts API Reference</p>"},{"location":"#what-odin-provides","title":"What Odin provides","text":"<p>Navigation</p> <p>Structure</p> <p>Semantics</p> <p>Search</p> <p>Patterns</p> <p>Zero-ops ML</p>"},{"location":"#guided-exploration","title":"Guided exploration","text":"<p>Odin is the compass, not the explorer. Given seed entities, it returns ranked, scored paths through the graph \u2014 leaving the agent to interpret what it finds instead of drowning in raw traversal.</p>"},{"location":"#personalized-pagerank","title":"Personalized PageRank","text":"<p>PPR identifies the structurally important nodes relative to your seeds, so exploration starts from the parts of the graph that actually matter.</p> <p>PPR concept \u2192</p>"},{"location":"#npll-edge-scoring","title":"NPLL edge scoring","text":"<p>Neural Probabilistic Logic Learning scores how plausible each edge is, filtering semantically invalid paths that naive traversal would follow.</p> <p>NPLL concept \u2192</p>"},{"location":"#beam-search","title":"Beam search","text":"<p>A bounded, best-first walk keeps multi-hop exploration tractable \u2014 top-K paths at each hop instead of exponential blow-up.</p> <p>Beam search \u2192</p>"},{"location":"#motifs-triage","title":"Motifs &amp; triage","text":"<p>Aggregation surfaces recurring motifs and produces a 0\u2013100 triage score, giving agents a single prioritization signal per retrieval.</p> <p>Aggregation \u2192</p>"},{"location":"#self-managing-model","title":"Self-managing model","text":"<p>Odin trains its NPLL model from your graph on first run and persists the weights in ArangoDB. No separate ML pipeline, no <code>.pt</code> files to manage.</p> <p>Model lifecycle \u2192</p>"},{"location":"#quickstart","title":"Quickstart","text":"Install<pre><code>pip install odin-engine\n</code></pre> explore.py<pre><code>from arango import ArangoClient\nfrom odin import OdinEngine\n\n# 1. Connect to your knowledge graph\nclient = ArangoClient(hosts=\"http://localhost:8529\")\ndb = client.db(\"my_graph\", username=\"root\", password=\"\")\n\n# 2. Initialize Odin (auto-trains NPLL from your graph on first run)\nengine = OdinEngine(db=db, community_id=\"global\")\n\n# 3. Explore from seed entities\nresult = engine.retrieve(\n    seeds=[\"entity/claim_123\", \"entity/provider_456\"],\n    max_paths=50,\n    hop_limit=3,\n)\n\n# 4. Read the ranked paths\nprint(f\"Triage score: {result['triage']['score']}/100\")\nfor path in result[\"paths\"][:5]:\n    nodes = \" \u2192 \".join(str(n) for n in path[\"nodes\"])\n    print(f\"  [{path['score']:.2f}] {nodes}\")\n</code></pre>"},{"location":"#where-to-start","title":"Where to start","text":"<p>If you are new to Odin, read in this order:</p> <ol> <li>Getting Started \u2014 install, connect ArangoDB, and run your first retrieval</li> <li>Architecture \u2014 the mental model for how the pipeline fits together</li> <li>Personalized PageRank \u2192 Beam Search \u2192 NPLL \u2014 the three scoring signals</li> <li>Triage &amp; Insight Scoring \u2014 how paths become a single prioritization number</li> </ol> <p>If you are evaluating for a specific use case:</p> <ul> <li>AI Agent Integration \u2014 wire Odin into an agent loop</li> <li>Healthcare Fraud Detection \u2014 a worked end-to-end example</li> <li>OdinEngine API \u2014 the full method surface</li> <li>Configuration \u2014 every parameter and its default</li> </ul>"},{"location":"#explore-the-ecosystem","title":"Explore the ecosystem","text":"Reference Full API surface, parameters, and result schema \u2014 view reference Source Browse the code, open issues, and submit PRs \u2014 GitHub PyPI Install the released package \u2014 odin-engine Prescott Data The team behind Odin \u2014 prescottdata.io"},{"location":"#star-us-on-github","title":"Star us on GitHub","text":"<p>Help more developers discover Odin. Every star makes the project easier to find and keeps it growing.</p> <p>Star on GitHub</p>"},{"location":"#build-something","title":"Build something","text":"<p>Odin is MIT-licensed and made to be extended \u2014 new adapters, aggregators, and PPR variants welcome.</p> <p>Read the guides</p>"},{"location":"changelog/","title":"Changelog","text":"<p>All notable changes to Odin-1 (<code>odin-engine</code>) are documented here.</p> <p>The format is based on Keep a Changelog, and this project adheres to Semantic Versioning.</p>"},{"location":"changelog/#unreleased","title":"Unreleased","text":""},{"location":"changelog/#021-2026-08-10","title":"0.2.1 - 2026-08-10","text":"<p>First release from the public Odin-1 repository (https://github.com/Prescott-Data/Odin-1).</p>"},{"location":"changelog/#changed","title":"Changed","text":"<ul> <li>Project metadata now points at the public GitHub repository and the   documentation site (<code>https://odin.developers.prescottdata.io</code>).</li> <li>Trimmed runtime requirements to the packages the engine actually imports   (<code>torch</code>, <code>python-arango</code>, <code>numpy</code>, <code>scipy</code>, <code>networkx</code>, <code>scikit-learn</code>,   <code>gremlinpython</code>).</li> </ul>"},{"location":"changelog/#added","title":"Added","text":"<ul> <li>Full documentation site (MkDocs Material).</li> <li><code>CHANGELOG.md</code>, <code>CONTRIBUTING.md</code>, and <code>CODE_OF_CONDUCT.md</code>.</li> <li>GitHub Actions for CI, PyPI publishing (trusted publishing), and docs   deployment.</li> </ul>"},{"location":"changelog/#notes","title":"Notes","text":"<ul> <li>No public API changes. Code is identical to the <code>0.2.0</code> release on PyPI.</li> </ul>"},{"location":"changelog/#020-2026-02-04","title":"0.2.0 - 2026-02-04","text":""},{"location":"changelog/#added_1","title":"Added","text":"<ul> <li>Runtime schema introspection via <code>SchemaInspector</code> and   <code>inspect_arango_schema</code>.</li> </ul>"},{"location":"changelog/#features-baseline","title":"Features (baseline)","text":"<ul> <li><code>OdinEngine</code> retrieval pipeline: Personalized PageRank -&gt; Beam Search -&gt;   NPLL edge scoring -&gt; aggregation (motifs, relation shares, triage score).</li> <li>Self-managing NPLL lifecycle (auto-train, weights persisted in ArangoDB).</li> <li>Edge plausibility scoring and PPR-based anchor discovery.</li> </ul>"},{"location":"getting-started/","title":"Getting Started","text":"<p>This guide takes you from a fresh Python environment to your first scored retrieval with Odin. It covers installation, connecting a graph database, and running the engine end to end.</p>"},{"location":"getting-started/#requirements","title":"Requirements","text":"<p>Odin requires Python 3.9 or later and a graph database. The reference backend is ArangoDB 3.10+.</p> Dependency Why Python \u2265 3.9 Runtime ArangoDB \u2265 3.10 Stores your knowledge graph and Odin's learned NPLL weights PyTorch \u2265 2.0 Powers the NPLL edge-plausibility model <p>PyTorch, <code>python-arango</code>, NumPy, scikit-learn, and <code>gremlinpython</code> are installed automatically with the package.</p>"},{"location":"getting-started/#installation","title":"Installation","text":"<pre><code>pip install odin-engine\n</code></pre> From PyPIFrom sourceWith dev tools <pre><code>pip install odin-engine\n</code></pre> <pre><code>git clone https://github.com/Prescott-Data/Odin-1.git\ncd Odin-1\npip install -e .\n</code></pre> <pre><code>pip install -e \".[dev]\"   # pytest, flake8, bandit\n</code></pre> <p>Import name</p> <p>The package is published as <code>odin-engine</code>, but you import it as <code>odin</code>:</p> <pre><code>from odin import OdinEngine, SchemaInspector\n</code></pre>"},{"location":"getting-started/#start-arangodb","title":"Start ArangoDB","text":"<p>If you do not already have a graph database, the fastest way to get one locally is Docker:</p> <pre><code>docker run -d --name arango -p 8529:8529 \\\n  -e ARANGO_NO_AUTH=1 arangodb:3.12\n</code></pre> <p>This exposes ArangoDB on <code>http://localhost:8529</code> with authentication disabled \u2014 fine for local development, never for production. See Connecting ArangoDB for authenticated setups.</p>"},{"location":"getting-started/#connect-and-initialize","title":"Connect and initialize","text":"<pre><code>from arango import ArangoClient\nfrom odin import OdinEngine\n\nclient = ArangoClient(hosts=\"http://localhost:8529\")\ndb = client.db(\"my_graph\", username=\"root\", password=\"\")\n\nengine = OdinEngine(db=db, community_id=\"global\")\n</code></pre> <p>On the first initialization against a graph, Odin extracts edge patterns and trains its NPLL model (typically 2\u20135 minutes). It then stores the learned weights in an ArangoDB collection, so subsequent runs load in about 30 seconds. See Model Lifecycle.</p> <p>No model? No problem</p> <p>If training cannot run (for example, an empty graph), Odin falls back to a constant edge-confidence and keeps working. Check <code>engine.has_npll</code> to see which mode you are in.</p>"},{"location":"getting-started/#run-your-first-retrieval","title":"Run your first retrieval","text":"<pre><code>result = engine.retrieve(\n    seeds=[\"entity/claim_123\"],\n    max_paths=50,\n    hop_limit=3,\n)\n\nprint(f\"Triage score: {result['triage']['score']}/100\")\nprint(f\"Paths found:  {len(result['paths'])}\")\n\nfor path in result[\"paths\"][:5]:\n    nodes = \" \u2192 \".join(str(n) for n in path[\"nodes\"])\n    print(f\"  [{path['score']:.2f}] {nodes}\")\n</code></pre> <p><code>retrieve()</code> runs the full pipeline \u2014 PPR \u2192 Beam Search \u2192 NPLL scoring \u2192 aggregation \u2014 and returns a dictionary of ranked paths, motifs, and scores. The full shape is documented in the Result Schema.</p>"},{"location":"getting-started/#score-a-single-edge","title":"Score a single edge","text":"<p>Beyond whole-path retrieval, you can ask Odin how plausible one relationship is:</p> <pre><code>score = engine.score_edge(\"entity/patient_001\", \"treated_by\", \"entity/doctor_smith\")\nprint(score)   # 0.0 (impossible) \u2026 1.0 (highly plausible)\n</code></pre> <p>This is the same NPLL signal Odin uses internally, exposed for use inside agent decision loops. See Scoring Edges.</p>"},{"location":"getting-started/#find-the-important-nodes","title":"Find the important nodes","text":"<pre><code>anchors = engine.find_anchors(seeds=[\"community/insurance_claims\"], topn=20)\nfor node_id, ppr_score in anchors[:10]:\n    print(f\"{ppr_score:.4f}  {node_id}\")\n</code></pre> <p><code>find_anchors()</code> returns the top-N nodes by Personalized PageRank relative to your seeds \u2014 a quick way to locate where to explore. See Finding Anchors.</p>"},{"location":"getting-started/#next-steps","title":"Next steps","text":"<ul> <li> <p> Connecting ArangoDB</p> <p>Authenticated connections, communities, and data layout.</p> <p>Read more \u2192</p> </li> <li> <p> Architecture</p> <p>How the retrieval pipeline is assembled.</p> <p>Read more \u2192</p> </li> <li> <p> AI Agent Integration</p> <p>Put Odin inside an agent's reasoning loop.</p> <p>Read more \u2192</p> </li> <li> <p> Tuning Retrieval</p> <p>Beam width, hop limits, and path budgets.</p> <p>Read more \u2192</p> </li> </ul>"},{"location":"concepts/","title":"Concepts","text":"<p>The ideas behind Odin \u2014 what it does, how the retrieval pipeline is assembled, and how each scoring signal contributes to the final ranking.</p> <p>Odin's job is narrow and deliberate: given seed entities, return the most relevant scored paths through a knowledge graph. It does not answer natural-language questions or generate prose \u2014 that is the agent's job. Odin is the compass; the agent is the explorer.</p> <ul> <li> <p> Architecture</p> <p>How PPR, beam search, NPLL, and aggregation compose into one pipeline.</p> <p>Read more \u2192</p> </li> <li> <p> Data Model</p> <p>Entities, relationships, and communities \u2014 how Odin sees your graph.</p> <p>Read more \u2192</p> </li> <li> <p> Personalized PageRank</p> <p>Finding the structurally important nodes relative to your seeds.</p> <p>Read more \u2192</p> </li> <li> <p> Beam Search</p> <p>Bounded, best-first multi-hop path exploration.</p> <p>Read more \u2192</p> </li> <li> <p> NPLL Edge Scoring</p> <p>Learned edge plausibility that filters invalid paths.</p> <p>Read more \u2192</p> </li> <li> <p> Motifs &amp; Aggregation</p> <p>Turning paths into recurring patterns and summaries.</p> <p>Read more \u2192</p> </li> <li> <p> Triage &amp; Insight Scoring</p> <p>Collapsing many signals into one 0\u2013100 prioritization number.</p> <p>Read more \u2192</p> </li> <li> <p> Caching</p> <p>Why graph access is LRU-cached and what it costs.</p> <p>Read more \u2192</p> </li> </ul>"},{"location":"concepts/#the-three-signals","title":"The three signals","text":"<p>Every path Odin returns is scored by combining three complementary signals:</p> Signal Question it answers Component Structural Is this node important in the graph? Personalized PageRank Reachable Can we get there efficiently? Beam Search Semantic Is this edge plausible? NPLL <p>No single signal is sufficient. PPR alone finds important nodes but follows nonsensical edges. Beam search alone explodes without a scoring signal to prune. NPLL alone validates edges but has no notion of importance. Together they let an agent focus on paths that are important, reachable, and plausible.</p>"},{"location":"concepts/aggregation/","title":"Motifs &amp; Aggregation","text":"<p>After beam search and NPLL scoring produce a set of surviving paths, Odin aggregates them into higher-level structure: recurring motifs, relation shares, and the summary statistics that feed the triage score.</p>"},{"location":"concepts/aggregation/#motifs","title":"Motifs","text":"<p>A motif is a recurring pattern across the returned paths \u2014 for example, the shape <code>Claim \u2192 billed_by \u2192 Provider \u2192 flagged_in \u2192 Audit</code> appearing many times. Motifs turn a list of individual paths into patterns, which is usually what an agent actually cares about.</p> <pre><code>motifs = result[\"aggregates\"][\"motifs\"]\nfor m in motifs:\n    print(m)   # e.g. {\"pattern\": \"A\u2192B\u2192C\", \"count\": 12}\n</code></pre> <p>Frequent motifs act as lightweight anomaly and pattern detection: if the same suspicious chain shows up 47 times, that is signal.</p>"},{"location":"concepts/aggregation/#relation-shares","title":"Relation shares","text":"<p><code>relation_share</code> reports how the returned edges break down by relation type \u2014 a quick fingerprint of what kind of connections dominate this retrieval. The most prominent one is surfaced as the <code>dominant_relation</code>.</p>"},{"location":"concepts/aggregation/#the-summary-block","title":"The summary block","text":"<p>Aggregation computes a <code>summary</code> of quality signals over the returned paths. These are the inputs to triage:</p> Field Meaning <code>total_paths</code> Number of paths returned <code>unique_motifs</code> Distinct motif shapes <code>unique_relations</code> Distinct relation types <code>total_edges</code> Edges across all paths <code>provenance</code> Fraction of edges with a source/provenance reference <code>recency</code> Recency-weighted freshness of the edges <code>label_coverage</code> Fraction of nodes/edges that are properly labeled <code>motif_density</code> How concentrated the results are into repeated motifs <code>has_baseline</code> Whether a baseline comparison was available <code>low_support</code> True when there is too little evidence to be confident <p><code>provenance</code> and <code>recency</code> reward well-sourced, fresh evidence; <code>label_coverage</code> guards against unlabeled noise; <code>low_support</code> flags thin results.</p>"},{"location":"concepts/aggregation/#baselines-and-surprise","title":"Baselines and surprise","text":"<p>When a baseline set of paths is available, aggregation compares the current retrieval against it to compute surprise \u2014 how much the dominant pattern deviates from the expected/prior distribution. Surprise is a major component of the triage score: a result that is unexpected is often the most worth an agent's attention.</p>"},{"location":"concepts/aggregation/#the-full-aggregates-object","title":"The full aggregates object","text":"<pre><code>result[\"aggregates\"] = {\n    \"motifs\":          [ {\"pattern\": \"...\", \"count\": N}, ... ],\n    \"relation_share\":  { \"billed_by\": 0.42, \"has_diagnosis\": 0.31, ... },\n    \"snippet_anchors\": [ ... ],\n    \"summary\":         { \"total_paths\": ..., \"provenance\": ..., ... },\n}\n</code></pre> <p>See the Result Schema for the complete return shape.</p>"},{"location":"concepts/aggregation/#next","title":"Next","text":"<ul> <li>Triage &amp; Insight Scoring \u2014 how these signals collapse into one number</li> </ul>"},{"location":"concepts/architecture/","title":"Architecture","text":"<p>Odin is a single library assembled from a small number of well-separated components. The public entry point is <code>OdinEngine</code>, which wires everything together and exposes a handful of methods.</p>"},{"location":"concepts/architecture/#the-pipeline","title":"The pipeline","text":"<p>A call to <code>retrieve()</code> runs four stages in sequence:</p> <pre><code>\u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510    \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510    \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510\n\u2502 Seed        \u2502 -&gt; \u2502 PPR \u2192 Beam Search \u2192 NPLL \u2192 Aggregate \u2502 -&gt; \u2502 Ranked Paths +  \u2502\n\u2502 Entities    \u2502    \u2502            (Odin Engine)             \u2502    \u2502 Motifs + Scores \u2502\n\u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518    \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518    \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n</code></pre> <ol> <li>PPR \u2014 Personalized PageRank scores nodes by structural importance relative to the seeds, producing anchor nodes to explore from.</li> <li>Beam Search \u2014 a bounded best-first walk expands the top-K paths at each hop, keeping the search tractable.</li> <li>NPLL scoring \u2014 each candidate edge is scored for plausibility; implausible paths are down-weighted or dropped.</li> <li>Aggregation \u2014 the surviving paths are summarized into motifs, relation shares, and a triage score.</li> </ol>"},{"location":"concepts/architecture/#components","title":"Components","text":"<pre><code>\u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510\n\u2502                              OdinEngine                                   \u2502\n\u2502                                                                           \u2502\n\u2502  \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510 \u2502\n\u2502  \u2502                    RetrievalOrchestrator                             \u2502 \u2502\n\u2502  \u2502  Coordinates PPR \u2192 Beam \u2192 NPLL \u2192 Aggregation and builds the result  \u2502 \u2502\n\u2502  \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518 \u2502\n\u2502                                                                           \u2502\n\u2502  \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510  \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510  \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510  \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510  \u2502\n\u2502  \u2502 Cached Graph \u2502  \u2502 APPRAnchors  \u2502  \u2502  NPLLModel   \u2502  \u2502 Aggregators  \u2502  \u2502\n\u2502  \u2502  Accessor    \u2502  \u2502   (PPR)      \u2502  \u2502 (confidence) \u2502  \u2502  (motifs)    \u2502  \u2502\n\u2502  \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518  \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518  \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518  \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518  \u2502\n\u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n</code></pre> Component Responsibility Source <code>OdinEngine</code> Public API; wiring and lifecycle <code>odin/engine.py</code> <code>RetrievalOrchestrator</code> Runs the pipeline, assembles the result <code>retrieval/orchestrator.py</code> <code>ArangoCommunityAccessor</code> Reads nodes/edges from ArangoDB <code>retrieval/adapters_arango.py</code> <code>CachedGraphAccessor</code> LRU cache over graph access <code>retrieval/cache.py</code> <code>APPRAnchors</code> Personalized PageRank anchors <code>retrieval/ppr/</code> <code>NPLLConfidence</code> / <code>ConstantConfidence</code> Edge plausibility scoring <code>retrieval/confidence.py</code> <code>NPLLModel</code> / <code>KnowledgeBootstrapper</code> Model and its lifecycle <code>npll/</code> aggregators Motifs, relation shares, triage <code>retrieval/aggregators.py</code>"},{"location":"concepts/architecture/#how-the-engine-is-wired","title":"How the engine is wired","text":"<p>When you construct an <code>OdinEngine</code>, it:</p> <ol> <li>Builds an <code>ArangoCommunityAccessor</code> for the given <code>community_id</code> and wraps it in a <code>CachedGraphAccessor</code>.</li> <li>Initializes intelligence \u2014 loads or trains the NPLL model via <code>KnowledgeBootstrapper</code> (unless <code>auto_train=False</code>), producing either <code>NPLLConfidence</code> or a <code>ConstantConfidence</code> fallback.</li> <li>Creates the <code>RetrievalOrchestrator</code> with the accessor and the confidence function.</li> <li>Creates the <code>APPRAnchors</code> engine for PPR queries.</li> </ol> <pre><code>engine = OdinEngine(\n    db=db,\n    community_id=\"global\",   # scope of exploration\n    cache_size=5000,          # graph-accessor LRU size\n    auto_train=True,          # train NPLL if no model exists\n    community_mode=\"none\",    # \"none\" = global, \"mapping\" = scoped\n)\n</code></pre> <p>See the OdinEngine API for every parameter.</p>"},{"location":"concepts/architecture/#separation-of-concerns","title":"Separation of concerns","text":"<p>Odin deliberately keeps graph intelligence separate from language reasoning:</p> <ul> <li>Odin returns structured, scored evidence \u2014 nodes, edges, motifs, and numbers.</li> <li>The agent (an LLM) interprets that evidence and decides what to do next.</li> </ul> <p>This boundary keeps Odin explainable and testable, and keeps hallucination out of the graph layer: every path Odin returns actually exists in your data.</p>"},{"location":"concepts/architecture/#where-to-go-next","title":"Where to go next","text":"<ul> <li>Data Model \u2014 how Odin reads your graph</li> <li>Personalized PageRank \u2014 the structural signal</li> <li>Result Schema \u2014 the exact shape returned by <code>retrieve()</code></li> </ul>"},{"location":"concepts/beam-search/","title":"Beam Search","text":"<p>Beam search is Odin's exploration strategy. It walks outward from PPR anchors to build multi-hop paths \u2014 without the exponential blow-up of naive traversal.</p>"},{"location":"concepts/beam-search/#the-problem-it-solves","title":"The problem it solves","text":"<p>A 3-hop exploration from a single node in a densely connected graph can generate 100K+ paths, the vast majority of which are noise. Breadth-first and depth-first search both explode; random walks never converge.</p> <p>Beam search keeps exploration bounded by only ever carrying forward the top-K partial paths at each hop.</p>"},{"location":"concepts/beam-search/#how-it-works","title":"How it works","text":"<p>At each hop:</p> <ol> <li>Take the current set of partial paths (the beam).</li> <li>Expand each one by its outgoing edges.</li> <li>Score every extended path (structural + semantic signals).</li> <li>Keep only the best <code>beam_width</code> paths.</li> <li>Repeat until <code>hop_limit</code> is reached or paths terminate.</li> </ol> <pre><code>hop 0:  [seed]\nhop 1:  keep top-K of  seed\u2192{a,b,c,d,...}\nhop 2:  keep top-K of  (top-K)\u2192{...}\nhop 3:  keep top-K of  (top-K)\u2192{...}\n</code></pre> <p>Because the beam is fixed-width, work per hop is bounded \u2014 turning a combinatorial explosion into a linear-in-hops search.</p>"},{"location":"concepts/beam-search/#the-two-knobs","title":"The two knobs","text":"Parameter Default Effect <code>beam_width</code> <code>64</code> How many partial paths survive each hop. Higher = broader, slower, more thorough. <code>hop_limit</code> <code>3</code> Maximum path length. Higher = deeper chains, more compute. <code>max_paths</code> <code>50</code> How many final paths to return. <p>Widening the beam trades compute for recall; deepening <code>hop_limit</code> finds longer causal chains at the cost of latency. See Tuning Retrieval for guidance.</p>"},{"location":"concepts/beam-search/#why-scoring-matters-mid-search","title":"Why scoring matters mid-search","text":"<p>Beam search is only as good as the signal it prunes with. Odin scores each candidate extension using both structural importance and NPLL edge plausibility \u2014 so an implausible edge (for example <code>Patient \u2192 diagnosed_by \u2192 Medication</code>) is pruned early rather than expanded into thousands of downstream nonsense paths.</p> <p>This tight coupling of search and semantics is what keeps Odin's output high-signal.</p>"},{"location":"concepts/beam-search/#budgets","title":"Budgets","text":"<p>Exploration runs under a budget so a single retrieval cannot run away. The budget usage is reported back in the result under <code>used_budget</code>, useful for observability and tuning.</p>"},{"location":"concepts/beam-search/#next","title":"Next","text":"<ul> <li>NPLL Edge Scoring \u2014 the plausibility signal that prunes the beam</li> <li>Motifs &amp; Aggregation \u2014 what happens to the surviving paths</li> </ul>"},{"location":"concepts/caching/","title":"Caching","text":"<p>Graph exploration touches the same nodes and edges repeatedly. Odin wraps every graph read in an LRU cache so retrieval stays fast on warm data.</p>"},{"location":"concepts/caching/#where-caching-happens","title":"Where caching happens","text":"<p>When you construct an <code>OdinEngine</code>, the raw ArangoDB accessor is wrapped in a <code>CachedGraphAccessor</code>:</p> <pre><code>engine = OdinEngine(db, cache_size=5000)   # LRU size for graph access\n</code></pre> <p>Every node fetch and neighbor lookup during PPR and beam search goes through this cache. The <code>cache_size</code> parameter controls how many entries are retained.</p>"},{"location":"concepts/caching/#two-layers-of-caching","title":"Two layers of caching","text":"Layer What it caches Controlled by Graph accessor Node and edge reads from the database <code>cache_size</code> (default <code>5000</code>) NPLL confidence Edge plausibility scores internal (<code>cache_size=10000</code>) <p>The NPLL confidence layer caches <code>score_edge</code> results, so repeatedly evaluating the same <code>(head, relation, tail)</code> during a search is effectively free after the first call.</p>"},{"location":"concepts/caching/#performance-impact","title":"Performance impact","text":"<p>On a warm cache, Odin reports &gt;80% hit rates after a short warm-up, contributing to the typical 300\u2013800 ms end-to-end retrieval latency for 50 paths at a 3-hop limit. Cold caches (the first retrieval after startup) are slower while the working set is populated.</p>"},{"location":"concepts/caching/#tuning-the-cache","title":"Tuning the cache","text":"Situation Recommendation Large, dense graph Increase <code>cache_size</code> to keep more of the working set resident Memory-constrained host Decrease <code>cache_size</code>; expect more database round-trips Many short, unrelated queries A smaller cache is fine \u2014 locality is low anyway <p>Memory usage scales with cache size and graph density \u2014 plan for roughly 500 MB\u20132 GB depending on both.</p>"},{"location":"concepts/caching/#freshness","title":"Freshness","text":"<p>The cache holds graph reads for the lifetime of the engine's working set. After significant data changes you should generally construct a fresh engine (and, if edge patterns changed materially, retrain the NPLL model) so stale structure does not linger.</p>"},{"location":"concepts/caching/#next","title":"Next","text":"<ul> <li>Tuning Retrieval \u2014 beam width, hops, and budgets</li> <li>Production Deployment \u2014 sizing for real workloads</li> </ul>"},{"location":"concepts/data-model/","title":"Data Model","text":"<p>Odin explores a directed, labeled knowledge graph: entities connected by typed relationships. This page describes what Odin expects from your data and the terms used throughout the docs.</p>"},{"location":"concepts/data-model/#entities-and-relationships","title":"Entities and relationships","text":"Term Meaning Example Entity (node) A thing in your domain, addressed by an ID <code>entity/claim_123</code>, <code>provider/456</code> Relationship (edge) A typed, directed connection between two entities <code>claim_123 \u2014billed_by\u2192 provider_456</code> Relation type The label on an edge <code>billed_by</code>, <code>has_diagnosis</code>, <code>treated_by</code> Path An ordered sequence of entities joined by edges <code>A \u2192 B \u2192 C</code> Seed An entity you start exploration from <code>[\"entity/claim_123\"]</code> <p>Node IDs are opaque strings. In ArangoDB they follow the <code>collection/key</code> convention (for example <code>ExtractedEntities/claim_123</code>), and Odin passes them through unchanged.</p>"},{"location":"concepts/data-model/#edges-carry-weight-and-confidence","title":"Edges carry weight and confidence","text":"<p>Each edge may carry metadata that Odin uses during scoring:</p> Field Used for <code>_from</code> / <code>_to</code> Direction of the relationship relation / type The relation label <code>weight</code> Optional structural weight <code>confidence</code> Optional pre-existing confidence (falls back to <code>weight</code>) <code>created_at</code> Optional recency signal in aggregation <code>provenance</code> Optional source/document reference <p>If confidence and weight are absent, Odin defaults them sensibly (see Aggregation).</p>"},{"location":"concepts/data-model/#communities","title":"Communities","text":"<p>A community is a named scope for exploration. It lets you partition a large graph so retrieval stays relevant to one tenant, dataset, or domain.</p> <ul> <li><code>community_mode=\"none\"</code> \u2014 global exploration across the whole graph (the default).</li> <li><code>community_mode=\"mapping\"</code> \u2014 scope queries to a specific <code>community_id</code>.</li> </ul> <pre><code># Global\nengine = OdinEngine(db, community_id=\"global\", community_mode=\"none\")\n\n# Scoped to one community\nengine = OdinEngine(db, community_id=\"medicare_claims\", community_mode=\"mapping\")\n</code></pre> <p>Communities are also the natural unit for NPLL: the model learns the edge patterns of the community it is trained on.</p>"},{"location":"concepts/data-model/#what-odin-does-not-require","title":"What Odin does not require","text":"<ul> <li>No fixed schema. Odin discovers collections and fields at runtime \u2014 see Schema Introspection.</li> <li>No pre-computed embeddings. NPLL trains directly from your graph's edge structure.</li> <li>No query language on your side. You pass entity IDs; Odin handles traversal.</li> </ul>"},{"location":"concepts/data-model/#backends","title":"Backends","text":"<p>The reference backend is ArangoDB (<code>retrieval/adapters_arango.py</code>). The accessor layer is an interface, so additional backends (Neo4j, Neptune, Gremlin-compatible stores) can be added by implementing the same node/edge access methods. Contributions of new adapters are welcome \u2014 see Contributing.</p>"},{"location":"concepts/data-model/#next","title":"Next","text":"<ul> <li>Personalized PageRank \u2014 how Odin ranks node importance</li> <li>Connecting ArangoDB \u2014 practical connection setup</li> </ul>"},{"location":"concepts/npll/","title":"NPLL Edge Scoring","text":"<p>NPLL \u2014 Neural Probabilistic Logic Learning \u2014 is Odin's semantic signal. It scores how plausible a given edge is, learned directly from the structure of your graph.</p>"},{"location":"concepts/npll/#what-it-does","title":"What it does","text":"<p>Given a candidate edge <code>(head, relation, tail)</code>, NPLL returns a probability between <code>0.0</code> and <code>1.0</code>:</p> <pre><code>score = engine.score_edge(\"entity/patient_001\", \"treated_by\", \"entity/doctor_smith\")\n# 0.0  \u2192 impossible / contradicts learned patterns\n# 1.0  \u2192 highly plausible / matches learned patterns\n</code></pre> <p>During retrieval this signal prunes semantically invalid paths \u2014 the ones naive traversal happily follows because the edge simply exists in the data, even when it makes no domain sense.</p>"},{"location":"concepts/npll/#why-learned-from-your-graph","title":"Why \"learned from your graph\"","text":"<p>NPLL does not ship with hand-written rules about medicine, finance, or supply chains. Instead it learns the plausibility patterns of your specific graph: which relation types connect which kinds of entities, and in which direction. A relationship that is common and consistent in your data scores high; a rare or contradictory one scores low.</p> <p>This makes the same engine work across domains without domain-specific configuration.</p>"},{"location":"concepts/npll/#self-managing-lifecycle","title":"Self-managing lifecycle","text":"<p>You never train NPLL by hand. On first initialization, <code>OdinEngine</code> bootstraps the model:</p> <ol> <li>First run \u2014 extract edge patterns from the graph and train the model (~2\u20135 minutes).</li> <li>Persist \u2014 save the learned weights into an ArangoDB collection.</li> <li>Subsequent runs \u2014 load the weights and rebuild the model in ~30 seconds.</li> </ol> <p>There is no separate ML pipeline, no <code>.pt</code> files to ship, and no DevOps overhead. See Model Lifecycle for the details and how to force a retrain.</p> <pre><code># Force a retrain after major graph changes\nengine.retrain_model()\n</code></pre>"},{"location":"concepts/npll/#graceful-fallback","title":"Graceful fallback","text":"<p>If a model cannot be trained or loaded (for example, an empty or brand-new graph), Odin does not fail. It falls back to a constant edge-confidence so retrieval keeps working, and reports the mode:</p> <pre><code>engine.has_npll        # True if the NPLL model is active\nengine.get_status()    # {'intelligence_mode': 'NPLL' | 'Constant', ...}\n</code></pre> <p>In constant mode you still get PPR-driven structural exploration; you just lose the semantic pruning until a model is available.</p>"},{"location":"concepts/npll/#where-the-signal-is-used","title":"Where the signal is used","text":"Use How Pruning during beam search Implausible extensions are dropped early Path scoring Contributes to each path's final score Direct edge validation <code>score_edge()</code> in your own agent logic"},{"location":"concepts/npll/#next","title":"Next","text":"<ul> <li>Model Lifecycle \u2014 training, persistence, and retraining</li> <li>Scoring Edges \u2014 using <code>score_edge()</code> in agent loops</li> <li>Triage &amp; Insight Scoring \u2014 how edge scores roll up into path scores</li> </ul>"},{"location":"concepts/ppr/","title":"Personalized PageRank","text":"<p>Personalized PageRank (PPR) is Odin's structural signal. It answers a single question: relative to my seed entities, which nodes in the graph matter most?</p>"},{"location":"concepts/ppr/#why-ppr","title":"Why PPR","text":"<p>Classic PageRank ranks every node by global importance. That is the wrong question for guided exploration \u2014 you do not care about globally famous nodes, you care about nodes important to your seeds.</p> <p>Personalized PageRank biases the random walk toward a set of seed nodes. The result is a per-seed importance distribution: nodes that are well-connected to the things you care about score highly, and distant or irrelevant nodes fade out.</p> <p>This is what lets Odin reduce the search space before it ever starts traversing \u2014 beam search then only expands from high-PPR anchors instead of the whole graph.</p>"},{"location":"concepts/ppr/#how-odin-uses-it","title":"How Odin uses it","text":"<p>Inside the pipeline, PPR runs first and produces anchors \u2014 the top nodes to explore from. You can also call it directly:</p> <pre><code>anchors = engine.find_anchors(seeds=[\"community/insurance_claims\"], topn=20)\nfor node_id, ppr_score in anchors:\n    print(f\"{ppr_score:.4f}  {node_id}\")\n</code></pre> <p><code>find_anchors()</code> returns a list of <code>(node_id, ppr_score)</code> tuples sorted by importance. See Finding Anchors for practical usage.</p>"},{"location":"concepts/ppr/#intuition","title":"Intuition","text":"<p>Think of PPR as releasing a large number of walkers at your seed nodes. Each walker follows edges at random, and with some probability teleports back to a seed. After many steps, the fraction of time spent at each node is its PPR score.</p> <ul> <li>Nodes tightly connected to seeds accumulate high scores.</li> <li>Bottleneck nodes that many paths pass through score highly \u2014 often the interesting connectors.</li> <li>Nodes far from every seed score near zero and are skipped.</li> </ul>"},{"location":"concepts/ppr/#parameters-that-matter","title":"Parameters that matter","text":"<p>PPR importance is shaped by the seeds you provide and the number of anchors you keep:</p> Knob Effect <code>seeds</code> The personalization vector \u2014 what importance is measured relative to <code>topn</code> How many anchors to return (via <code>find_anchors</code>) <code>community_id</code> / <code>community_mode</code> Restricts the walk to a scope <p>More seeds broaden the notion of \"important\"; fewer, more specific seeds produce sharper, more targeted anchors.</p>"},{"location":"concepts/ppr/#caching","title":"Caching","text":"<p>PPR is computed over the cached graph accessor, so repeated retrievals against a warm cache are fast. See Caching.</p>"},{"location":"concepts/ppr/#next","title":"Next","text":"<ul> <li>Beam Search \u2014 how anchors become multi-hop paths</li> <li>Finding Anchors \u2014 the practical guide</li> </ul>"},{"location":"concepts/scoring/","title":"Triage &amp; Insight Scoring","text":"<p>A retrieval can return dozens of paths and motifs. To make that actionable, Odin collapses it into a single triage score from 0\u2013100 \u2014 the number an agent uses to decide how much attention this result deserves.</p>"},{"location":"concepts/scoring/#the-triage-score","title":"The triage score","text":"<p><code>result[\"triage\"][\"score\"]</code> is an integer in <code>[0, 100]</code>, computed from five weighted components:</p> <p>$$ \\text{score} = 25\\,p + 25\\,r + 25\\,s + 15\\,m + 10\\,c $$</p> Symbol Component Weight Rewards $p$ <code>provenance</code> 25 Well-sourced edges with references $r$ <code>recency</code> 25 Fresh, recently-updated evidence $s$ <code>surprise</code> 25 Deviation from the baseline / prior $m$ <code>motif_density</code> 15 Concentration into repeated patterns $c$ <code>controllability</code> 10 How actionable the finding is <p>All components are in <code>[0, 1]</code>, so the maximum possible score is 100.</p>"},{"location":"concepts/scoring/#guards","title":"Guards","text":"<p>Two guards keep the score honest:</p> <ul> <li>Low label coverage \u2014 if <code>label_coverage &lt; 0.8</code>, <code>motif_density</code> is capped at <code>0.3</code> and 15 points are subtracted. Poorly-labeled data cannot earn full pattern credit.</li> <li>Low support \u2014 if <code>low_support</code> is set (too little evidence), the score is reduced by 40%. Thin results are not allowed to look confident.</li> </ul> <pre><code>triage = result[\"triage\"]\nprint(triage[\"score\"])        # e.g. 87\nprint(triage[\"components\"])   # per-component breakdown, penalties, flags\nprint(triage[\"dominant_relation\"])\n</code></pre>"},{"location":"concepts/scoring/#reading-the-components","title":"Reading the components","text":"<p>The <code>components</code> dict returns each clamped input, the applied <code>penalty</code>, and the <code>low_support</code> flag \u2014 so a score is never a black box. If a score is surprisingly low, inspect the components to see whether provenance, recency, or a guard pulled it down.</p> <pre><code>{\n  \"provenance\": 0.82,\n  \"recency\": 0.74,\n  \"surprise\": 0.61,\n  \"motif_density\": 0.30,     # possibly capped by the label-coverage guard\n  \"controllability\": 1.0,\n  \"label_coverage\": 0.71,\n  \"penalty\": 15.0,           # label-coverage guard fired\n  \"low_support\": false\n}\n</code></pre>"},{"location":"concepts/scoring/#insight-score-vs-triage-score","title":"Insight score vs. triage score","text":"<p>Odin also reports an <code>insight_score</code> (a <code>[0, 1]</code> float) alongside <code>evidence_strength</code> and <code>community_relevance</code>. The <code>ics</code> field decomposes the insight score into these contributing parts.</p> Field Range Use <code>triage[\"score\"]</code> 0\u2013100 Prioritization for agents \u2014 \"should I look at this?\" <code>insight_score</code> 0.0\u20131.0 Overall retrieval quality signal <code>evidence_strength</code> 0.0\u20131.0 Strength of the supporting evidence <code>community_relevance</code> 0.0\u20131.0 How relevant the result is to the community scope <code>ics</code> object Decomposition of <code>insight_score</code> <p>For most agent loops, the triage score is the number to gate on; the others are available when you need finer-grained observability.</p>"},{"location":"concepts/scoring/#using-it-in-an-agent","title":"Using it in an agent","text":"<pre><code>result = engine.retrieve(seeds=[...])\nif result[\"triage\"][\"score\"] &gt;= 70:\n    agent.investigate(result[\"paths\"])\nelse:\n    agent.skip(reason=\"low triage\")\n</code></pre> <p>See AI Agent Integration for a complete loop.</p>"},{"location":"concepts/scoring/#next","title":"Next","text":"<ul> <li>Result Schema \u2014 the exact return shape</li> <li>Tuning Retrieval \u2014 how parameters affect the scores</li> </ul>"},{"location":"examples/","title":"Examples","text":"<p>Worked, end-to-end examples of Odin applied to real problem shapes. Each one uses the same three-signal pipeline \u2014 only the seeds and interpretation change.</p> <ul> <li> <p> Healthcare Fraud Detection</p> <p>Surface providers billing unusual procedure combinations.</p> <p>Read more \u2192</p> </li> <li> <p> Supply Chain Risk</p> <p>Trace cascading dependencies across supplier tiers.</p> <p>Read more \u2192</p> </li> <li> <p> Regulatory Compliance</p> <p>Validate entity relationships against compliance rules.</p> <p>Read more \u2192</p> </li> </ul>"},{"location":"examples/#the-common-shape","title":"The common shape","text":"<p>Every example follows the same skeleton:</p> <pre><code>from arango import ArangoClient\nfrom odin import OdinEngine\n\ndb = ArangoClient(hosts=\"http://localhost:8529\").db(\n    \"my_graph\", username=\"root\", password=\"\"\n)\nengine = OdinEngine(db, community_id=\"...\", community_mode=\"mapping\")\n\nresult = engine.retrieve(seeds=[...], max_paths=..., hop_limit=...)\n# \u2192 inspect result[\"triage\"], result[\"paths\"], result[\"aggregates\"][\"motifs\"]\n</code></pre> <p>What differs between domains is the seeds (where you start), the hop limit (how far the interesting chains reach), and how the agent interprets the returned motifs.</p>"},{"location":"examples/compliance/","title":"Regulatory Compliance","text":"<p>Scenario: validate entity relationships against compliance rules \u2014 for example, detecting when a fund is connected, directly or indirectly, to a sanctioned entity.</p>"},{"location":"examples/compliance/#the-graph","title":"The graph","text":"<p>A compliance graph contains entities like <code>Fund</code>, <code>Manager</code>, <code>Entity</code>, <code>Person</code>, and <code>Jurisdiction</code>, connected by relations such as <code>managed_by</code>, <code>owns</code>, <code>affiliated_with</code>, and <code>controlled_by</code>. Prohibited relationships are often indirect \u2014 hidden a few hops away.</p>"},{"location":"examples/compliance/#direct-rule-check-with-score_edge","title":"Direct rule check with <code>score_edge</code>","text":"<p>For a specific prohibited relationship, score it directly:</p> <pre><code>from arango import ArangoClient\nfrom odin import OdinEngine\n\ndb = ArangoClient(hosts=\"http://localhost:8529\").db(\n    \"compliance\", username=\"root\", password=\"\"\n)\nengine = OdinEngine(db, community_id=\"compliance\", community_mode=\"mapping\")\n\nscore = engine.score_edge(\n    \"entity/investment_fund\",\n    \"managed_by\",\n    \"entity/sanctioned_entity\",\n)\nif score &gt; 0.5:\n    compliance_agent.flag_for_review(\n        \"Fund plausibly managed by a sanctioned entity\"\n    )\n</code></pre> <p><code>score_edge()</code> gives you the NPLL plausibility of a single relationship \u2014 a fast gate for rule checks. See Scoring Edges.</p>"},{"location":"examples/compliance/#find-indirect-exposure-with-retrieval","title":"Find indirect exposure with retrieval","text":"<p>Direct edges are the easy case. To catch indirect ties, retrieve paths from the fund and look for any that reach a sanctioned entity:</p> <pre><code>result = engine.retrieve(\n    seeds=[\"entity/investment_fund\"],\n    hop_limit=4,          # indirect control can be several hops away\n    max_paths=100,\n)\n\nfor p in result[\"paths\"]:\n    if any(\"sanctioned\" in str(n) for n in p[\"nodes\"]):\n        compliance_agent.flag_for_review(p)\n</code></pre> <p>This finds chains like <code>Fund \u2192 managed_by \u2192 Manager \u2192 affiliated_with \u2192 Sanctioned Entity</code> that a single-edge check would miss.</p>"},{"location":"examples/compliance/#why-npll-helps-compliance","title":"Why NPLL helps compliance","text":"Concern How Odin helps False positives from coincidental edges NPLL down-weights implausible connections Hidden, indirect relationships Multi-hop beam search surfaces them Explainability for auditors Every flagged path is a real, inspectable chain in the graph <p>Because Odin only returns paths that exist in your data, every flag is auditable \u2014 there is no hallucinated relationship to defend.</p>"},{"location":"examples/compliance/#escalation-gate","title":"Escalation gate","text":"<pre><code>score = result[\"triage\"][\"score\"]\nif score &gt;= 75:\n    compliance_agent.escalate(result)     # strong, well-sourced exposure\nelif score &gt;= 40:\n    human_review.enqueue(result)          # uncertain \u2014 needs an analyst\n</code></pre> <p>See AI Agent Integration for the human-in-the-loop pattern.</p>"},{"location":"examples/compliance/#next","title":"Next","text":"<ul> <li>Scoring Edges</li> <li>Examples overview</li> </ul>"},{"location":"examples/healthcare-fraud/","title":"Healthcare Fraud Detection","text":"<p>Scenario: find providers billing unusual procedure combinations that may indicate fraud, waste, or abuse.</p>"},{"location":"examples/healthcare-fraud/#the-graph","title":"The graph","text":"<p>A claims knowledge graph typically contains entities like <code>Provider</code>, <code>Claim</code>, <code>Patient</code>, <code>Diagnosis</code>, <code>Procedure</code> (CPT codes), and <code>Audit</code>, connected by relations such as <code>billed_by</code>, <code>has_diagnosis</code>, <code>has_procedure</code>, and <code>flagged_in</code>.</p>"},{"location":"examples/healthcare-fraud/#explore-from-a-suspicious-provider","title":"Explore from a suspicious provider","text":"<pre><code>from arango import ArangoClient\nfrom odin import OdinEngine\n\ndb = ArangoClient(hosts=\"http://localhost:8529\").db(\n    \"claims\", username=\"root\", password=\"\"\n)\nengine = OdinEngine(db, community_id=\"medicare_claims\", community_mode=\"mapping\")\n\nresult = engine.retrieve(\n    seeds=[\"provider/high_volume_clinic\"],\n    max_paths=100,\n    hop_limit=3,\n)\n\nprint(f\"Triage: {result['triage']['score']}/100\")\nfor m in result[\"aggregates\"][\"motifs\"][:5]:\n    print(m)\n# e.g. {\"pattern\": \"CPT_99285 + CPT_office_visit\", \"count\": 34}\n</code></pre> <p>Odin surfaces the recurring billing pattern \u2014 \"CPT_99285 paired with an office visit appears in 34 claims\" \u2014 as a motif, without anyone writing a rule for that specific combination in advance.</p>"},{"location":"examples/healthcare-fraud/#why-the-three-signals-matter-here","title":"Why the three signals matter here","text":"Signal In this domain PPR Finds the high-volume providers and central claims worth examining NPLL Rejects nonsensical edges (a procedure is not <code>diagnosed_by</code> a patient), keeping paths clinically coherent Motifs Turns thousands of claims into a handful of repeated billing patterns"},{"location":"examples/healthcare-fraud/#validate-a-specific-concern","title":"Validate a specific concern","text":"<p>If an analyst suspects a particular relationship, score it directly:</p> <pre><code>score = engine.score_edge(\n    \"provider/high_volume_clinic\", \"billed\", \"procedure/CPT_99285\"\n)\nif score &gt; 0.7:\n    analyst.flag(\"Unusually strong billing tie to high-acuity code\")\n</code></pre>"},{"location":"examples/healthcare-fraud/#hand-off-to-an-agent","title":"Hand off to an agent","text":"<pre><code>if result[\"triage\"][\"score\"] &gt;= 70:\n    fraud_agent.reason(\n        prompt=\"Explain why this provider's billing pattern is anomalous.\",\n        evidence=result[\"paths\"][:10],\n        motifs=result[\"aggregates\"][\"motifs\"][:5],\n    )\n</code></pre> <p>The agent writes the narrative; Odin guarantees the evidence is real and ranked. See AI Agent Integration.</p>"},{"location":"examples/healthcare-fraud/#tuning-notes","title":"Tuning notes","text":"<ul> <li>Keep <code>hop_limit=3</code> \u2014 fraud patterns are usually provider \u2192 claim \u2192 code/audit, a few hops.</li> <li>Widen <code>beam_width</code> if you suspect patterns are being missed across many claims.</li> <li>Scope with <code>community_mode=\"mapping\"</code> so the NPLL model learns this payer's billing norms.</li> </ul>"},{"location":"examples/healthcare-fraud/#next","title":"Next","text":"<ul> <li>Supply Chain Risk</li> <li>Regulatory Compliance</li> </ul>"},{"location":"examples/supply-chain/","title":"Supply Chain Risk","text":"<p>Scenario: identify cascading supplier dependencies \u2014 where a problem at a deep-tier supplier ripples up to many downstream products.</p>"},{"location":"examples/supply-chain/#the-graph","title":"The graph","text":"<p>A supply-chain graph contains entities like <code>Supplier</code>, <code>Component</code>, <code>Product</code>, <code>Facility</code>, and <code>Region</code>, connected by relations such as <code>supplies</code>, <code>depends_on</code>, <code>manufactured_at</code>, and <code>ships_to</code>. Risk often hides several tiers deep.</p>"},{"location":"examples/supply-chain/#explore-deep-dependencies","title":"Explore deep dependencies","text":"<p>Because supply chains are deep, this is a case for a higher <code>hop_limit</code>:</p> <pre><code>from arango import ArangoClient\nfrom odin import OdinEngine\n\ndb = ArangoClient(hosts=\"http://localhost:8529\").db(\n    \"supply\", username=\"root\", password=\"\"\n)\nengine = OdinEngine(db, community_id=\"supply\", community_mode=\"mapping\")\n\nresult = engine.retrieve(\n    seeds=[\"supplier/critical_vendor\"],\n    hop_limit=5,          # deep exploration across tiers\n    max_paths=100,\n)\n\nfor p in result[\"paths\"][:10]:\n    print(f\"[{p['score']:.2f}]\", \" -&gt; \".join(str(n) for n in p[\"nodes\"]))\n# Discovers: a Tier-3 supplier feeds 47 downstream products\n</code></pre> <p>The deep walk reveals chains like <code>Tier-3 supplier \u2192 component \u2192 sub-assembly \u2192 product</code>, quantifying how far a single vendor's disruption propagates.</p>"},{"location":"examples/supply-chain/#why-deeper-hops-here","title":"Why deeper hops here","text":"Domain trait Odin setting Risk is many tiers deep <code>hop_limit=5</code> (or more) Chains fan out widely Narrow the <code>beam_width</code> to keep depth affordable One vendor, many products Seed on the vendor; read node frequencies in the aggregates <p>See Tuning Retrieval for the depth-vs-breadth trade-off.</p>"},{"location":"examples/supply-chain/#rank-the-exposure","title":"Rank the exposure","text":"<p>Use anchors to find the most structurally central suppliers before drilling in:</p> <pre><code>anchors = engine.find_anchors(seeds=[\"region/southeast_asia\"], topn=20)\nfor node_id, ppr in anchors[:10]:\n    print(f\"{ppr:.4f}  {node_id}\")   # the load-bearing suppliers in the region\n</code></pre> <p>High-PPR suppliers are the ones whose failure would affect the most paths \u2014 the priorities for a resilience review.</p>"},{"location":"examples/supply-chain/#hand-off-to-an-agent","title":"Hand off to an agent","text":"<pre><code>if result[\"triage\"][\"score\"] &gt;= 65:\n    risk_agent.reason(\n        prompt=\"Summarize the single-point-of-failure risk in this supply chain.\",\n        evidence=result[\"paths\"][:10],\n    )\n</code></pre>"},{"location":"examples/supply-chain/#next","title":"Next","text":"<ul> <li>Healthcare Fraud Detection</li> <li>Regulatory Compliance</li> </ul>"},{"location":"guides/","title":"Guides","text":"<p>Practical, task-focused walkthroughs. Each guide assumes you have installed <code>odin-engine</code> and can reach a graph database \u2014 see Getting Started if not.</p> <ul> <li> <p> Connecting ArangoDB</p> <p>Authenticated connections, databases, and communities.</p> <p>Read more \u2192</p> </li> <li> <p> Your First Retrieval</p> <p>Run the full pipeline and read every field of the result.</p> <p>Read more \u2192</p> </li> <li> <p> Scoring Edges</p> <p>Use <code>score_edge()</code> to validate relationships in agent logic.</p> <p>Read more \u2192</p> </li> <li> <p> Finding Anchors</p> <p>Locate the important nodes with Personalized PageRank.</p> <p>Read more \u2192</p> </li> <li> <p> Schema Introspection</p> <p>Discover collections and fields at runtime for agents and docs.</p> <p>Read more \u2192</p> </li> <li> <p> Model Lifecycle</p> <p>Training, persistence, and retraining the NPLL model.</p> <p>Read more \u2192</p> </li> <li> <p> Tuning Retrieval</p> <p>Beam width, hop limits, and path budgets.</p> <p>Read more \u2192</p> </li> <li> <p> AI Agent Integration</p> <p>Put Odin inside an agent's reasoning loop.</p> <p>Read more \u2192</p> </li> <li> <p> Production Deployment</p> <p>Sizing, caching, and operating Odin at scale.</p> <p>Read more \u2192</p> </li> </ul>"},{"location":"guides/agent-integration/","title":"AI Agent Integration","text":"<p>Odin is built to sit inside an AI agent's reasoning loop. It supplies structured, scored evidence; the agent supplies the language reasoning. This guide shows the integration patterns.</p>"},{"location":"guides/agent-integration/#the-division-of-labor","title":"The division of labor","text":"Odin (the compass) The agent (the explorer) Finds important nodes (PPR) Decides what to investigate Explores paths (beam search) Interprets what paths mean Scores plausibility (NPLL) Writes conclusions in natural language Prioritizes (triage) Takes actions / escalates <p>Odin never hallucinates a relationship \u2014 every path it returns exists in your graph.</p>"},{"location":"guides/agent-integration/#pattern-1-retrieve-then-reason","title":"Pattern 1 \u2014 Retrieve, then reason","text":"<p>The core loop: retrieve scored evidence, gate on the triage score, and hand the surviving paths to the LLM.</p> <pre><code>def investigate(engine, agent, seeds):\n    result = engine.retrieve(seeds=seeds, max_paths=50, hop_limit=3)\n\n    if result[\"triage\"][\"score\"] &lt; 60:\n        return agent.note(\"Low-signal region; skipping.\")\n\n    evidence = format_paths(result[\"paths\"][:10])\n    motifs = result[\"aggregates\"][\"motifs\"][:5]\n    return agent.reason(\n        prompt=\"Analyze these graph findings and explain the key risk.\",\n        evidence=evidence,\n        motifs=motifs,\n    )\n</code></pre>"},{"location":"guides/agent-integration/#pattern-2-validate-the-agents-hypotheses","title":"Pattern 2 \u2014 Validate the agent's hypotheses","text":"<p>When an LLM proposes a relationship, check it against the graph before acting with <code>score_edge()</code>:</p> <pre><code>hypothesis = agent.propose_relationship()   # (src, rel, dst)\nif engine.score_edge(*hypothesis) &gt; 0.7:\n    agent.act_on(hypothesis)\nelse:\n    agent.reconsider(hypothesis, reason=\"not supported by the graph\")\n</code></pre> <p>This closes the loop between an agent's free-form reasoning and verifiable graph structure.</p>"},{"location":"guides/agent-integration/#pattern-3-give-the-agent-graph-context","title":"Pattern 3 \u2014 Give the agent graph context","text":"<p>Use schema introspection to prime the agent so it can request the right seeds or write valid queries:</p> <pre><code>from odin import inspect_arango_schema\n\ninspect_arango_schema(db, output_file=\"schema.json\")\nagent.load_context(\"schema.json\")   # now the agent knows the collections/fields\n</code></pre>"},{"location":"guides/agent-integration/#pattern-4-human-in-the-loop-on-the-margin","title":"Pattern 4 \u2014 Human-in-the-loop on the margin","text":"<p>The triage score gives you a clean three-way gate \u2014 act, skip, or escalate:</p> <pre><code>score = result[\"triage\"][\"score\"]\nif score &gt;= 75:\n    agent.act(result)\nelif score &lt;= 40:\n    agent.skip(result)\nelse:\n    human_review.enqueue(result)   # uncertain \u2014 ask a person\n</code></pre>"},{"location":"guides/agent-integration/#formatting-evidence-for-an-llm","title":"Formatting evidence for an LLM","text":"<p>Keep the payload compact and readable \u2014 paths as arrows, plus the motifs and the score:</p> <pre><code>def format_paths(paths):\n    lines = []\n    for p in paths:\n        chain = \" -&gt; \".join(str(n) for n in p[\"nodes\"])\n        lines.append(f\"[{p['score']:.2f}] {chain}\")\n    return \"\\n\".join(lines)\n</code></pre> <p>You can also let the agent pull the raw context itself: Odin returns everything as plain Python data, so it serializes cleanly to JSON for tool calls.</p>"},{"location":"guides/agent-integration/#reference-architecture","title":"Reference architecture","text":"<pre><code>        seeds\n          \u2502\n          \u25bc\n   \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510   scored paths + triage    \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510\n   \u2502    Odin      \u2502 \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u25b6 \u2502    Agent     \u2502\n   \u2502 (evidence)   \u2502 \u25c0\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500 \u2502  (reasoning) \u2502\n   \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518   score_edge(hypothesis)   \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n</code></pre> <p>For the full technical write-up, see the Agent Integration Guide in the repository.</p>"},{"location":"guides/agent-integration/#next","title":"Next","text":"<ul> <li>Scoring Edges</li> <li>Examples</li> </ul>"},{"location":"guides/anchors/","title":"Finding Anchors","text":"<p>Anchors are the structurally important nodes relative to your seeds, found with Personalized PageRank. They tell an agent where to look before any traversal happens.</p>"},{"location":"guides/anchors/#the-method","title":"The method","text":"<pre><code>anchors = engine.find_anchors(seeds, topn=20)\n</code></pre> Argument Default Meaning <code>seeds</code> \u2014 Seed entities for personalized PageRank <code>topn</code> <code>20</code> How many top nodes to return <p>Returns a list of <code>(node_id, ppr_score)</code> tuples, sorted by importance (highest first).</p> <pre><code>anchors = engine.find_anchors(seeds=[\"community/insurance_claims\"], topn=20)\nfor node_id, ppr_score in anchors[:10]:\n    print(f\"{ppr_score:.4f}  {node_id}\")\n</code></pre>"},{"location":"guides/anchors/#when-to-use-anchors","title":"When to use anchors","text":"Use case Why anchors help Targeted retrieval Feed high-PPR anchors back in as seeds for a focused second pass Graph orientation Show an agent the neighborhood's key players before deciding what to explore Budget control Explore only from the top anchors instead of the entire frontier"},{"location":"guides/anchors/#anchor-then-retrieve-pattern","title":"Anchor-then-retrieve pattern","text":"<p>A common two-step flow: discover the important nodes, then explore from the strongest ones.</p> <pre><code># 1. Find the important nodes around a broad seed\nanchors = engine.find_anchors(seeds=[\"community/insurance_claims\"], topn=10)\n\n# 2. Retrieve paths from the strongest anchors\nstrong = [node_id for node_id, score in anchors[:3]]\nresult = engine.retrieve(seeds=strong, max_paths=50, hop_limit=3)\n</code></pre> <p>This narrows a broad starting point down to the parts of the graph that actually matter before spending exploration budget.</p>"},{"location":"guides/anchors/#choosing-topn","title":"Choosing <code>topn</code>","text":"<code>topn</code> Effect Small (5\u201310) Sharp focus on the few most important nodes Medium (20) Balanced neighborhood view (default) Large (50+) Broad map of the region; more compute downstream"},{"location":"guides/anchors/#inspecting-a-nodes-neighborhood","title":"Inspecting a node's neighborhood","text":"<p>To look at one node in detail rather than rank many, use <code>get_neighbors()</code>:</p> <pre><code>info = engine.get_neighbors(\"entity/provider_456\")\nprint(info[\"degree\"])\nfor n in info[\"neighbors\"][:10]:\n    print(n[\"direction\"], n[\"rel\"], n[\"id\"], n[\"weight\"])\n</code></pre>"},{"location":"guides/anchors/#next","title":"Next","text":"<ul> <li>Personalized PageRank \u2014 the concept behind anchors</li> <li>Your First Retrieval</li> </ul>"},{"location":"guides/arangodb/","title":"Connecting ArangoDB","text":"<p>Odin's reference backend is ArangoDB. This guide covers connecting, choosing a database, and scoping exploration to a community.</p>"},{"location":"guides/arangodb/#install-the-driver","title":"Install the driver","text":"<p>The ArangoDB Python driver (<code>python-arango</code>) is installed automatically with <code>odin-engine</code>. You import it as <code>arango</code>:</p> <pre><code>from arango import ArangoClient\n</code></pre>"},{"location":"guides/arangodb/#basic-connection","title":"Basic connection","text":"<pre><code>from arango import ArangoClient\nfrom odin import OdinEngine\n\nclient = ArangoClient(hosts=\"http://localhost:8529\")\ndb = client.db(\"my_graph\", username=\"root\", password=\"\")\n\nengine = OdinEngine(db=db)\n</code></pre> <p><code>OdinEngine</code> takes an already-connected <code>StandardDatabase</code> object \u2014 it never manages credentials itself. That keeps secrets in your connection code and out of Odin.</p>"},{"location":"guides/arangodb/#authenticated-production-connections","title":"Authenticated / production connections","text":"<p>Never disable authentication outside local development. Use a dedicated, least-privilege user and pull secrets from the environment:</p> <pre><code>import os\nfrom arango import ArangoClient\n\nclient = ArangoClient(hosts=os.environ[\"ARANGO_HOSTS\"])   # e.g. https://db.internal:8529\ndb = client.db(\n    os.environ[\"ARANGO_DB\"],\n    username=os.environ[\"ARANGO_USER\"],\n    password=os.environ[\"ARANGO_PASSWORD\"],\n)\n</code></pre> <p>Secrets</p> <p>Do not hard-code passwords in source or notebooks. Load them from environment variables or a secrets manager. Odin never logs your credentials.</p>"},{"location":"guides/arangodb/#local-database-with-docker","title":"Local database with Docker","text":"<pre><code>docker run -d --name arango -p 8529:8529 \\\n  -e ARANGO_NO_AUTH=1 arangodb:3.12\n</code></pre> <p>For an authenticated local instance, use <code>ARANGO_ROOT_PASSWORD</code> instead of <code>ARANGO_NO_AUTH</code>:</p> <pre><code>docker run -d --name arango -p 8529:8529 \\\n  -e ARANGO_ROOT_PASSWORD=change-me arangodb:3.12\n</code></pre>"},{"location":"guides/arangodb/#scoping-to-a-community","title":"Scoping to a community","text":"<p>A community restricts exploration to a named subset of the graph:</p> <pre><code># Global exploration (default)\nengine = OdinEngine(db, community_id=\"global\", community_mode=\"none\")\n\n# Scoped exploration\nengine = OdinEngine(db, community_id=\"medicare_claims\", community_mode=\"mapping\")\n</code></pre> <p>Use <code>community_mode=\"mapping\"</code> when you have partitioned a large multi-tenant graph and want retrieval \u2014 and the NPLL model \u2014 focused on one partition.</p>"},{"location":"guides/arangodb/#verifying-the-connection","title":"Verifying the connection","text":"<pre><code>print(engine.get_status())\n# {'community_id': 'global', 'npll_loaded': True,\n#  'intelligence_mode': 'NPLL', 'cache_size': 5000}\n</code></pre> <p>If <code>intelligence_mode</code> is <code>Constant</code>, the NPLL model has not trained yet \u2014 see Model Lifecycle.</p>"},{"location":"guides/arangodb/#other-backends","title":"Other backends","text":"<p>The accessor layer is an interface (<code>retrieval/adapters.py</code>). Backends beyond ArangoDB can be added by implementing the same node/edge access contract. See Contributing.</p>"},{"location":"guides/arangodb/#next","title":"Next","text":"<ul> <li>Your First Retrieval</li> <li>Schema Introspection</li> </ul>"},{"location":"guides/edge-scoring/","title":"Scoring Edges","text":"<p>Beyond whole-path retrieval, Odin can score a single relationship. This is the NPLL plausibility signal exposed directly, for use inside your own agent logic.</p>"},{"location":"guides/edge-scoring/#the-method","title":"The method","text":"<pre><code>score = engine.score_edge(src, rel, dst)\n</code></pre> Argument Meaning <code>src</code> Source entity ID <code>rel</code> Relation type <code>dst</code> Destination entity ID <p>Returns a <code>float</code> in <code>[0.0, 1.0]</code>:</p> <ul> <li><code>0.0</code> \u2014 impossible / contradicts learned patterns</li> <li><code>1.0</code> \u2014 highly plausible / matches learned patterns</li> </ul> <pre><code>engine.score_edge(\"entity/patient_001\", \"treated_by\", \"entity/doctor_smith\")\n# 0.91\nengine.score_edge(\"entity/patient_001\", \"diagnosed_by\", \"entity/aspirin\")\n# 0.03   \u2190 implausible: a patient is not diagnosed_by a medication\n</code></pre>"},{"location":"guides/edge-scoring/#using-it-in-a-decision-loop","title":"Using it in a decision loop","text":"<p>The typical pattern is a plausibility gate before acting on a candidate relationship:</p> <pre><code>def maybe_investigate(agent, src, rel, dst):\n    score = engine.score_edge(src, rel, dst)\n    if score &gt; 0.7:\n        agent.investigate_further(src, rel, dst)\n    elif score &lt; 0.2:\n        agent.discard(src, rel, dst, reason=\"implausible edge\")\n    else:\n        agent.request_human_review(src, rel, dst, confidence=score)\n</code></pre>"},{"location":"guides/edge-scoring/#validating-a-hypothesis","title":"Validating a hypothesis","text":"<p><code>score_edge()</code> is a fast way to check a relationship an agent proposes but has not confirmed in the graph \u2014 for example, validating an LLM's guess before writing it back:</p> <pre><code>proposed = (\"entity/fund_A\", \"managed_by\", \"entity/sanctioned_entity\")\nif engine.score_edge(*proposed) &gt; 0.5:\n    compliance_agent.flag_for_review(proposed)\n</code></pre>"},{"location":"guides/edge-scoring/#performance","title":"Performance","text":"<p>Edge scores are cached (see Caching), so repeatedly scoring the same triple within a session is effectively free after the first call \u2014 roughly ~5 ms per edge cached.</p>"},{"location":"guides/edge-scoring/#when-npll-is-unavailable","title":"When NPLL is unavailable","text":"<p>If the engine is running in constant-confidence mode (<code>engine.has_npll == False</code>), <code>score_edge()</code> returns the constant fallback rather than a learned score. Check the mode before relying on fine-grained plausibility:</p> <pre><code>if engine.has_npll:\n    score = engine.score_edge(src, rel, dst)\nelse:\n    # No trained model yet \u2014 see the Model Lifecycle guide\n    ...\n</code></pre> <p>See Model Lifecycle to ensure a model is trained.</p>"},{"location":"guides/edge-scoring/#next","title":"Next","text":"<ul> <li>NPLL Edge Scoring \u2014 the concept behind the score</li> <li>AI Agent Integration \u2014 full agent loops</li> </ul>"},{"location":"guides/first-retrieval/","title":"Your First Retrieval","text":"<p>This guide runs the full Odin pipeline and walks through every field of the result so you know exactly what you are getting back.</p>"},{"location":"guides/first-retrieval/#run-a-retrieval","title":"Run a retrieval","text":"<pre><code>from arango import ArangoClient\nfrom odin import OdinEngine\n\nclient = ArangoClient(hosts=\"http://localhost:8529\")\ndb = client.db(\"my_graph\", username=\"root\", password=\"\")\n\nengine = OdinEngine(db=db, community_id=\"global\")\n\nresult = engine.retrieve(\n    seeds=[\"entity/claim_123\"],\n    max_paths=50,\n    hop_limit=3,\n    beam_width=64,\n)\n</code></pre> <p><code>retrieve()</code> runs PPR \u2192 beam search \u2192 NPLL scoring \u2192 aggregation and returns a single dictionary.</p>"},{"location":"guides/first-retrieval/#parameters","title":"Parameters","text":"Parameter Type Default Meaning <code>seeds</code> <code>list[str]</code> \u2014 Entity IDs to start from <code>max_paths</code> <code>int</code> <code>50</code> Maximum paths to return <code>hop_limit</code> <code>int</code> <code>3</code> Maximum path length <code>beam_width</code> <code>int</code> <code>64</code> Paths kept per hop during beam search <p>See Tuning Retrieval for how these interact.</p>"},{"location":"guides/first-retrieval/#the-result-at-a-glance","title":"The result at a glance","text":"<pre><code>result.keys()\n# dict_keys(['topk_ppr', 'paths', 'evidence_strength',\n#            'community_relevance', 'insight_score', 'aggregates',\n#            'triage', 'ics', 'used_budget', 'trace'])\n</code></pre>"},{"location":"guides/first-retrieval/#ranked-paths","title":"Ranked paths","text":"<pre><code>for path in result[\"paths\"][:5]:\n    nodes = \" \u2192 \".join(str(n) for n in path[\"nodes\"])\n    print(f\"[{path['score']:.2f}] {nodes}\")\n</code></pre> <p>Each path has <code>nodes</code>, <code>edges</code>, and a combined <code>score</code>. Paths are returned best-first.</p>"},{"location":"guides/first-retrieval/#triage-score","title":"Triage score","text":"<pre><code>result[\"triage\"][\"score\"]         # 0\u2013100 prioritization number\nresult[\"triage\"][\"components\"]    # per-component breakdown\n</code></pre> <p>This is usually the number you gate on. See Triage &amp; Insight Scoring.</p>"},{"location":"guides/first-retrieval/#anchors-ppr","title":"Anchors (PPR)","text":"<pre><code>result[\"topk_ppr\"]   # the structurally important nodes used as anchors\n</code></pre>"},{"location":"guides/first-retrieval/#aggregates","title":"Aggregates","text":"<pre><code>result[\"aggregates\"][\"motifs\"]          # recurring patterns\nresult[\"aggregates\"][\"relation_share\"]  # relation-type breakdown\nresult[\"aggregates\"][\"summary\"]         # provenance, recency, coverage, ...\n</code></pre>"},{"location":"guides/first-retrieval/#quality-signals","title":"Quality signals","text":"<pre><code>result[\"insight_score\"]         # 0.0\u20131.0 overall quality\nresult[\"evidence_strength\"]     # 0.0\u20131.0\nresult[\"community_relevance\"]   # 0.0\u20131.0\nresult[\"ics\"]                   # decomposition of insight_score\n</code></pre>"},{"location":"guides/first-retrieval/#observability","title":"Observability","text":"<pre><code>result[\"used_budget\"]   # how much exploration budget was consumed\nresult[\"trace\"]         # ppr/beam traces, params, timings_ms\n</code></pre> <p>The full, authoritative shape is documented in the Result Schema.</p>"},{"location":"guides/first-retrieval/#a-complete-example","title":"A complete example","text":"<pre><code>result = engine.retrieve(seeds=[\"entity/claim_123\"], max_paths=25)\n\nprint(f\"Triage: {result['triage']['score']}/100   \"\n      f\"Insight: {result['insight_score']:.2f}   \"\n      f\"Paths: {len(result['paths'])}\")\n\nprint(\"\\nTop motifs:\")\nfor m in result[\"aggregates\"][\"motifs\"][:5]:\n    print(\"  \", m)\n\nprint(\"\\nTop paths:\")\nfor p in result[\"paths\"][:5]:\n    print(f\"  [{p['score']:.2f}]\", \" \u2192 \".join(str(n) for n in p[\"nodes\"]))\n</code></pre>"},{"location":"guides/first-retrieval/#next","title":"Next","text":"<ul> <li>Scoring Edges</li> <li>Tuning Retrieval</li> <li>AI Agent Integration</li> </ul>"},{"location":"guides/npll-lifecycle/","title":"Model Lifecycle","text":"<p>Odin's NPLL model is self-managing: it trains itself from your graph, persists to the database, and reloads automatically. This guide explains the lifecycle and how to control it.</p>"},{"location":"guides/npll-lifecycle/#the-three-phases","title":"The three phases","text":"<pre><code>first run \u2500\u2500\u25b6 train (2\u20135 min) \u2500\u2500\u25b6 persist weights \u2500\u2500\u25b6 ArangoDB\n                                                          \u2502\nlater runs \u25c0\u2500\u2500 rebuild (~30 s) \u25c0\u2500\u2500 load weights \u25c0\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n</code></pre> <ol> <li>First run \u2014 when you construct an <code>OdinEngine</code> and no model exists, <code>KnowledgeBootstrapper</code> extracts edge patterns from the graph and trains the NPLL model (typically 2\u20135 minutes).</li> <li>Persist \u2014 the learned weights are stored in an ArangoDB collection. There are no <code>.pt</code> files to manage.</li> <li>Subsequent runs \u2014 the weights are loaded from the database and the model is rebuilt in about 30 seconds.</li> </ol>"},{"location":"guides/npll-lifecycle/#controlling-training-at-startup","title":"Controlling training at startup","text":"<pre><code># Default: train automatically if no model exists\nengine = OdinEngine(db, auto_train=True)\n\n# Disable training \u2014 use a constant-confidence fallback instead\nengine = OdinEngine(db, auto_train=False)\n</code></pre> <p>With <code>auto_train=False</code>, Odin skips NPLL entirely and uses a constant edge-confidence. You still get PPR-driven structural exploration, but not semantic pruning.</p>"},{"location":"guides/npll-lifecycle/#checking-the-mode","title":"Checking the mode","text":"<pre><code>engine.has_npll          # True if the NPLL model is active\nengine.get_status()\n# {'community_id': 'global', 'npll_loaded': True,\n#  'intelligence_mode': 'NPLL', 'cache_size': 5000}\n</code></pre> <p>If <code>intelligence_mode</code> is <code>Constant</code>, either training was disabled, or it could not complete (for example, an empty graph).</p>"},{"location":"guides/npll-lifecycle/#forcing-a-retrain","title":"Forcing a retrain","text":"<p>After significant graph changes \u2014 new relation types, a large data load, or a structural shift \u2014 retrain so the model reflects current patterns:</p> <pre><code>ok = engine.retrain_model()   # returns True on success\n</code></pre> <p><code>retrain_model()</code> retrains from scratch, persists the new weights, and rebuilds the engine's confidence and orchestrator to use them.</p> <p>When to retrain</p> <p>You do not need to retrain for ordinary incremental writes. Retrain when the shape of the graph changes \u2014 new kinds of entities or relationships \u2014 not merely when a few documents are added.</p>"},{"location":"guides/npll-lifecycle/#per-community-models","title":"Per-community models","text":"<p>Because NPLL learns the patterns of the community it trains on, a model is tied to its <code>community_id</code>. If you serve multiple communities, each gets its own model, trained the first time you initialize an engine for that community.</p> <pre><code>claims = OdinEngine(db, community_id=\"claims\", community_mode=\"mapping\")\nsupply = OdinEngine(db, community_id=\"supply\", community_mode=\"mapping\")\n# Each trains/loads its own NPLL model on first use.\n</code></pre>"},{"location":"guides/npll-lifecycle/#failure-handling","title":"Failure handling","text":"<p>NPLL initialization is defensive: if training or loading raises, Odin logs the error and falls back to constant confidence rather than failing the whole engine. Inspect logs under the <code>odin</code> logger to see what happened, and confirm the mode with <code>get_status()</code>.</p>"},{"location":"guides/npll-lifecycle/#next","title":"Next","text":"<ul> <li>NPLL Edge Scoring</li> <li>Production Deployment</li> </ul>"},{"location":"guides/production/","title":"Production Deployment","text":"<p>Guidance for running Odin against real graphs at scale \u2014 sizing, caching, model management, and operability.</p>"},{"location":"guides/production/#tested-scale","title":"Tested scale","text":"<p>Odin has been run on graphs from 10K to 5M entities. Representative deployments:</p> Graph Entities Edges Typical retrieval Healthcare KG 2.3M 8.7M ~450 ms Insurance claims 850K 4.1M ~320 ms Supply chain 450K 2.3M ~280 ms <p>Typical end-to-end latency is 300\u2013800 ms for a 50-path retrieval at a 3-hop limit.</p>"},{"location":"guides/production/#resource-sizing","title":"Resource sizing","text":"Resource Guidance Memory ~500 MB\u20132 GB depending on <code>cache_size</code> and graph density CPU PPR and beam search are CPU-bound; scale horizontally for concurrency PyTorch Required for NPLL; CPU inference is sufficient for scoring Cache Aim for a warm cache (&gt;80% hit rate) before measuring latency <p>Tune <code>cache_size</code> to hold the working set of your typical queries \u2014 see Caching.</p>"},{"location":"guides/production/#model-management","title":"Model management","text":"<ul> <li>Warm-up: the first retrieval after startup trains or loads the NPLL model. Loading a persisted model takes ~30 s; training from scratch takes 2\u20135 min. Warm the engine before serving traffic.</li> <li>Weights live in the database: there are no model files to ship or mount. Ensure the ArangoDB user can read/write the weights collection.</li> <li>Retrain on structural change, not on every write \u2014 see Model Lifecycle.</li> </ul>"},{"location":"guides/production/#concurrency","title":"Concurrency","text":"<p>An <code>OdinEngine</code> holds a cache and a loaded model. For a service:</p> <ul> <li>Construct one engine per community and reuse it across requests.</li> <li>Do not construct a new engine per request \u2014 you would pay warm-up costs every time.</li> <li>For multi-tenant serving, keep a small pool of engines keyed by <code>community_id</code>.</li> </ul>"},{"location":"guides/production/#security","title":"Security","text":"<ul> <li>Least privilege: give Odin a dedicated ArangoDB user scoped to the database it needs.</li> <li>Secrets from the environment: never hard-code credentials; Odin takes an already-connected <code>db</code> handle and never logs credentials.</li> <li>Network: keep the database on a private network; terminate TLS in front of ArangoDB in production.</li> </ul> <p>See Connecting ArangoDB.</p>"},{"location":"guides/production/#observability","title":"Observability","text":"<p>Every result carries a trace you can log or export:</p> <pre><code>result[\"trace\"][\"timings_ms\"]   # per-stage timings incl. 'total'\nresult[\"used_budget\"]           # exploration budget consumed\nresult[\"insight_score\"]         # quality signal for dashboards\n</code></pre> <p>Emit <code>triage[\"score\"]</code> and <code>timings_ms[\"total\"]</code> as metrics to watch signal quality and latency over time.</p>"},{"location":"guides/production/#containerizing","title":"Containerizing","text":"<p>A minimal image is provided in the repository (<code>Dockerfile</code>). Install the package and your app on top of a slim Python base:</p> <pre><code>FROM python:3.11-slim\nRUN pip install --no-cache-dir odin-engine\n# ... copy your service code, set env, run\n</code></pre> <p>Provide the ArangoDB connection via environment variables at runtime.</p>"},{"location":"guides/production/#pre-flight-checklist","title":"Pre-flight checklist","text":"<ul> <li>[ ] Dedicated, least-privilege ArangoDB user</li> <li>[ ] Credentials injected via environment / secrets manager</li> <li>[ ] Engine constructed once per community and reused</li> <li>[ ] Engine warmed (model loaded) before serving</li> <li>[ ] <code>cache_size</code> tuned to the working set</li> <li>[ ] <code>timings_ms</code> and <code>triage.score</code> exported as metrics</li> <li>[ ] Retrain strategy defined for structural graph changes</li> </ul>"},{"location":"guides/production/#next","title":"Next","text":"<ul> <li>Tuning Retrieval</li> <li>Troubleshooting</li> </ul>"},{"location":"guides/schema-introspection/","title":"Schema Introspection","text":"<p><code>SchemaInspector</code> discovers your ArangoDB structure at runtime \u2014 every collection, its fields, and how edge collections connect. It is designed to give AI agents the context they need to write valid queries, and to auto-generate documentation.</p>"},{"location":"guides/schema-introspection/#why-introspection","title":"Why introspection","text":"<p>Agents that write their own AQL need to know what collections and fields exist. Rather than hard-coding a schema, <code>SchemaInspector</code> reads it live, so it stays correct as your graph evolves.</p>"},{"location":"guides/schema-introspection/#basic-usage","title":"Basic usage","text":"<pre><code>from arango import ArangoClient\nfrom odin import SchemaInspector\n\nclient = ArangoClient(hosts=\"http://localhost:8529\")\ndb = client.db(\"my_graph\", username=\"root\", password=\"\")\n\ninspector = SchemaInspector(db)\nschema = inspector.get_schema_map()\n\nprint(f\"Database: {schema['database_name']}\")\nprint(f\"Collections: {len(schema['collections'])}\")\nprint(f\"Edge collections: {len(schema['edges'])}\")\n</code></pre> <p><code>get_schema_map()</code> returns a dictionary with <code>database_name</code>, <code>collections</code>, and <code>edges</code>. It is cached after the first call; pass <code>refresh=True</code> to rebuild it.</p>"},{"location":"guides/schema-introspection/#collection-details","title":"Collection details","text":"<pre><code>for col in schema[\"collections\"]:\n    print(f\"{col['name']}: {col['count']} docs\")\n    print(f\"  fields: {', '.join(col['fields'][:5])}\")\n</code></pre> <p>Each document collection reports its <code>name</code>, <code>type</code>, <code>count</code>, and discovered <code>fields</code>.</p> <pre><code>info = inspector.get_collection_info(\"ExtractedEntities\")\nprint(info[\"fields\"])\n</code></pre>"},{"location":"guides/schema-introspection/#edge-relationships","title":"Edge relationships","text":"<p>Edge collections additionally report which collections they connect:</p> <pre><code>edge = inspector.get_edge_info(\"ExtractedRelationships\")\nprint(\"From:\", edge[\"from_collections\"])\nprint(\"To:  \", edge[\"to_collections\"])\nprint(\"Fields:\", edge[\"fields\"])\n</code></pre> <p>This <code>_from</code>/<code>_to</code> mapping is exactly what an agent needs to construct a valid traversal.</p>"},{"location":"guides/schema-introspection/#save-schema-for-agents-or-docs","title":"Save schema for agents or docs","text":"<pre><code>from odin import inspect_arango_schema\n\ninspect_arango_schema(db, output_file=\"schema.json\")\n</code></pre> <p>Use the exported file to:</p> <ul> <li>Prime an agent with graph context in its system prompt.</li> <li>Auto-generate database schema documentation.</li> <li>Validate that collection structures match across environments.</li> <li>Track schema evolution over time in version control.</li> </ul>"},{"location":"guides/schema-introspection/#sampling-behavior","title":"Sampling behavior","text":"<p>Field discovery samples a small number of documents per collection (<code>max_sample_docs</code>, default <code>5</code>). Increase it when documents are highly heterogeneous:</p> <pre><code>inspector = SchemaInspector(db, max_sample_docs=25)\n</code></pre> <p>System collections (names starting with <code>_</code>) are skipped automatically.</p>"},{"location":"guides/schema-introspection/#next","title":"Next","text":"<ul> <li>SchemaInspector API \u2014 full method reference</li> <li>AI Agent Integration</li> </ul>"},{"location":"guides/tuning/","title":"Tuning Retrieval","text":"<p>Odin's retrieval behavior is governed by a few parameters. This guide explains what each one does, how they interact, and how to trade recall for latency.</p>"},{"location":"guides/tuning/#the-parameters","title":"The parameters","text":"Parameter Where Default Trades <code>beam_width</code> <code>retrieve()</code> <code>64</code> Recall \u2194 latency <code>hop_limit</code> <code>retrieve()</code> <code>3</code> Depth \u2194 latency <code>max_paths</code> <code>retrieve()</code> <code>50</code> Output size <code>cache_size</code> <code>OdinEngine()</code> <code>5000</code> Memory \u2194 database round-trips <code>community_mode</code> <code>OdinEngine()</code> <code>\"none\"</code> Scope of exploration"},{"location":"guides/tuning/#beam-width","title":"Beam width","text":"<p><code>beam_width</code> is how many partial paths survive each hop of beam search.</p> <ul> <li>Wider (128, 256) \u2192 higher recall, finds more of the relevant paths, more compute.</li> <li>Narrower (16, 32) \u2192 faster, sharper, may miss relevant-but-lower-scored paths.</li> </ul> <p>Start at the default <code>64</code>; widen only if you observe that relevant paths are being missed.</p>"},{"location":"guides/tuning/#hop-limit","title":"Hop limit","text":"<p><code>hop_limit</code> is the maximum path length.</p> <code>hop_limit</code> Use for 2 Direct relationships and immediate context 3 (default) Most retrieval \u2014 good depth without explosion 4\u20135 Deep causal chains, e.g. multi-tier supply chains <p>Each additional hop increases cost roughly with the beam width, so raise <code>hop_limit</code> deliberately.</p>"},{"location":"guides/tuning/#how-they-interact","title":"How they interact","text":"<p>Latency is driven primarily by <code>beam_width \u00d7 hop_limit</code>. Two rules of thumb:</p> <ul> <li>To go deeper, consider narrowing the beam so total work stays bounded.</li> <li>To go broader, keep hops shallow and widen the beam.</li> </ul> <pre><code># Deep but focused\nengine.retrieve(seeds, hop_limit=5, beam_width=24)\n\n# Shallow but thorough\nengine.retrieve(seeds, hop_limit=2, beam_width=128)\n</code></pre>"},{"location":"guides/tuning/#seeds-matter-most","title":"Seeds matter most","text":"<p>The single biggest lever is seed quality. Specific, relevant seeds produce sharp, high-triage results; broad seeds dilute the PPR signal. When results feel noisy, tighten the seeds \u2014 or run an anchor-then-retrieve pass first.</p>"},{"location":"guides/tuning/#reading-the-budget","title":"Reading the budget","text":"<p>Every result reports how much exploration budget it consumed and where time went:</p> <pre><code>result[\"used_budget\"]              # budget consumed\nresult[\"trace\"][\"timings_ms\"]      # per-stage timings, including 'total'\n</code></pre> <p>Use these to see whether PPR, beam search, or scoring dominates your latency before changing parameters.</p>"},{"location":"guides/tuning/#a-tuning-workflow","title":"A tuning workflow","text":"<ol> <li>Start at defaults (<code>beam_width=64</code>, <code>hop_limit=3</code>).</li> <li>Check <code>triage[\"score\"]</code> and whether expected paths appear.</li> <li>If recall is low \u2192 widen the beam or improve seeds.</li> <li>If latency is high \u2192 inspect <code>timings_ms</code>, then narrow the beam or reduce hops.</li> <li>If memory is high \u2192 lower <code>cache_size</code> (accepting more DB round-trips).</li> </ol>"},{"location":"guides/tuning/#next","title":"Next","text":"<ul> <li>Caching</li> <li>Production Deployment</li> </ul>"},{"location":"reference/","title":"Reference","text":"<p>The complete API surface, parameters, and data shapes.</p> <ul> <li> <p> OdinEngine API</p> <p>Every method on the main entry point.</p> <p>Read more \u2192</p> </li> <li> <p> SchemaInspector API</p> <p>Runtime schema discovery.</p> <p>Read more \u2192</p> </li> <li> <p> Configuration</p> <p>All constructor and retrieval parameters with defaults.</p> <p>Read more \u2192</p> </li> <li> <p> Result Schema</p> <p>The exact dictionary returned by <code>retrieve()</code>.</p> <p>Read more \u2192</p> </li> <li> <p> Troubleshooting</p> <p>Common issues and how to resolve them.</p> <p>Read more \u2192</p> </li> </ul>"},{"location":"reference/#public-imports","title":"Public imports","text":"<pre><code>from odin import OdinEngine, SchemaInspector, inspect_arango_schema\n</code></pre> Symbol Purpose <code>OdinEngine</code> Main entry point \u2014 retrieval, scoring, anchors <code>SchemaInspector</code> Runtime ArangoDB schema discovery <code>inspect_arango_schema</code> One-call schema export helper <p><code>odin.__version__</code> reports the installed version.</p>"},{"location":"reference/configuration/","title":"Configuration","text":"<p>All configuration is passed as arguments \u2014 there are no global settings or environment variables that Odin reads on its own. This page collects every parameter in one place.</p>"},{"location":"reference/configuration/#engine-construction","title":"Engine construction","text":"<p><code>OdinEngine(...)</code> \u2014 see the OdinEngine API.</p> Parameter Type Default Notes <code>db</code> <code>StandardDatabase</code> \u2014 Connected <code>python-arango</code> handle <code>community_id</code> <code>str</code> <code>\"global\"</code> Exploration scope <code>cache_size</code> <code>int</code> <code>5000</code> Graph-accessor LRU size \u2014 see Caching <code>auto_train</code> <code>bool</code> <code>True</code> Train NPLL on first run if no model exists <code>community_mode</code> <code>str</code> <code>\"none\"</code> <code>\"none\"</code> = global, <code>\"mapping\"</code> = scoped"},{"location":"reference/configuration/#retrieval","title":"Retrieval","text":"<p><code>retrieve(...)</code> \u2014 see Tuning Retrieval.</p> Parameter Type Default Effect <code>seeds</code> <code>list[str]</code> \u2014 Where exploration starts <code>max_paths</code> <code>int</code> <code>50</code> Output size <code>hop_limit</code> <code>int</code> <code>3</code> Maximum path length <code>beam_width</code> <code>int</code> <code>64</code> Paths kept per hop"},{"location":"reference/configuration/#anchors","title":"Anchors","text":"<p><code>find_anchors(...)</code></p> Parameter Type Default <code>seeds</code> <code>list[str]</code> \u2014 <code>topn</code> <code>int</code> <code>20</code>"},{"location":"reference/configuration/#schema-inspection","title":"Schema inspection","text":"<p><code>SchemaInspector(...)</code></p> Parameter Type Default <code>db</code> <code>StandardDatabase</code> \u2014 <code>max_sample_docs</code> <code>int</code> <code>5</code> <p><code>inspect_arango_schema(db, output_file=\"schema.json\")</code>.</p>"},{"location":"reference/configuration/#database-connection-yours","title":"Database connection (yours)","text":"<p>Odin does not manage the connection \u2014 you construct it with <code>python-arango</code>. In production, source these from the environment:</p> Variable (suggested) Used for <code>ARANGO_HOSTS</code> ArangoDB URL(s) <code>ARANGO_DB</code> Database name <code>ARANGO_USER</code> Username <code>ARANGO_PASSWORD</code> Password (from a secrets manager) <pre><code>import os\nfrom arango import ArangoClient\n\ndb = ArangoClient(hosts=os.environ[\"ARANGO_HOSTS\"]).db(\n    os.environ[\"ARANGO_DB\"],\n    username=os.environ[\"ARANGO_USER\"],\n    password=os.environ[\"ARANGO_PASSWORD\"],\n)\n</code></pre> <p>See Connecting ArangoDB.</p>"},{"location":"reference/configuration/#requirements","title":"Requirements","text":"Requirement Version Python \u2265 3.9 ArangoDB \u2265 3.10 PyTorch \u2265 2.0 <p>Runtime dependencies (<code>torch</code>, <code>python-arango</code>, <code>numpy</code>, <code>scipy</code>, <code>networkx</code>, <code>scikit-learn</code>, <code>gremlinpython</code>) install automatically with <code>pip install odin-engine</code>.</p>"},{"location":"reference/engine/","title":"OdinEngine API","text":"<p><code>OdinEngine</code> is the main entry point. Construct it with a connected ArangoDB database, then call its methods.</p> <pre><code>from odin import OdinEngine\n</code></pre>"},{"location":"reference/engine/#constructor","title":"Constructor","text":"<pre><code>OdinEngine(\n    db,\n    community_id: str = \"global\",\n    cache_size: int = 5000,\n    auto_train: bool = True,\n    community_mode: str = \"none\",\n)\n</code></pre> Parameter Type Default Description <code>db</code> <code>StandardDatabase</code> \u2014 A connected <code>python-arango</code> database instance <code>community_id</code> <code>str</code> <code>\"global\"</code> Scope to explore within <code>cache_size</code> <code>int</code> <code>5000</code> LRU size for the graph accessor <code>auto_train</code> <code>bool</code> <code>True</code> Train NPLL if no model exists <code>community_mode</code> <code>str</code> <code>\"none\"</code> <code>\"none\"</code> = global, <code>\"mapping\"</code> = community-scoped <p>On first construction against a graph, NPLL trains (2\u20135 min) unless <code>auto_train=False</code>. See Model Lifecycle.</p>"},{"location":"reference/engine/#retrieve","title":"<code>retrieve</code>","text":"<pre><code>retrieve(\n    seeds: list[str],\n    max_paths: int = 50,\n    hop_limit: int = 3,\n    beam_width: int = 64,\n) -&gt; dict\n</code></pre> <p>Runs the full pipeline (PPR \u2192 beam search \u2192 NPLL scoring \u2192 aggregation) and returns scored paths.</p> Parameter Type Default Description <code>seeds</code> <code>list[str]</code> \u2014 Starting entity IDs <code>max_paths</code> <code>int</code> <code>50</code> Maximum paths to return <code>hop_limit</code> <code>int</code> <code>3</code> Maximum path length <code>beam_width</code> <code>int</code> <code>64</code> Paths kept per hop <p>Returns: a <code>dict</code> \u2014 see the Result Schema.</p> <pre><code>result = engine.retrieve(seeds=[\"entity/claim_123\"], max_paths=50)\nresult[\"triage\"][\"score\"]     # 0\u2013100\nresult[\"paths\"]               # ranked paths\n</code></pre>"},{"location":"reference/engine/#score_edge","title":"<code>score_edge</code>","text":"<pre><code>score_edge(src: str, rel: str, dst: str) -&gt; float\n</code></pre> <p>Returns the NPLL plausibility of a single edge, from <code>0.0</code> (impossible) to <code>1.0</code> (highly plausible).</p> <pre><code>engine.score_edge(\"entity/patient_001\", \"treated_by\", \"entity/doctor_smith\")\n# 0.91\n</code></pre> <p>See Scoring Edges.</p>"},{"location":"reference/engine/#find_anchors","title":"<code>find_anchors</code>","text":"<pre><code>find_anchors(seeds: list[str], topn: int = 20) -&gt; list[tuple[str, float]]\n</code></pre> <p>Returns the top-N nodes by Personalized PageRank relative to <code>seeds</code>, as <code>(node_id, ppr_score)</code> tuples sorted by importance.</p> <pre><code>for node_id, ppr in engine.find_anchors([\"community/claims\"], topn=20):\n    print(ppr, node_id)\n</code></pre> <p>See Finding Anchors.</p>"},{"location":"reference/engine/#get_neighbors","title":"<code>get_neighbors</code>","text":"<pre><code>get_neighbors(node_id: str) -&gt; dict\n</code></pre> <p>Returns a node and its neighbors with relation types and directions.</p> <pre><code>info = engine.get_neighbors(\"entity/provider_456\")\ninfo[\"degree\"]        # total neighbor count\ninfo[\"neighbors\"]     # list of {\"id\", \"rel\", \"weight\", \"direction\"}\n</code></pre> <p>Each neighbor's <code>direction</code> is <code>\"out\"</code> or <code>\"in\"</code>.</p>"},{"location":"reference/engine/#retrain_model","title":"<code>retrain_model</code>","text":"<pre><code>retrain_model() -&gt; bool\n</code></pre> <p>Forces a full NPLL retrain, persists the new weights, and rebuilds the engine's scoring. Returns <code>True</code> on success. Use after structural graph changes \u2014 see Model Lifecycle.</p>"},{"location":"reference/engine/#has_npll","title":"<code>has_npll</code>","text":"<pre><code>has_npll -&gt; bool     # property\n</code></pre> <p><code>True</code> when a trained NPLL model is active; <code>False</code> in constant-confidence fallback.</p>"},{"location":"reference/engine/#get_status","title":"<code>get_status</code>","text":"<pre><code>get_status() -&gt; dict\n</code></pre> <p>Returns a small status dictionary:</p> <pre><code>{\n    \"community_id\": \"global\",\n    \"npll_loaded\": True,\n    \"intelligence_mode\": \"NPLL\",   # or \"Constant\"\n    \"cache_size\": 5000,\n}\n</code></pre>"},{"location":"reference/engine/#method-summary","title":"Method summary","text":"Method Returns Purpose <code>retrieve(...)</code> <code>dict</code> Full pipeline: ranked, scored paths <code>score_edge(src, rel, dst)</code> <code>float</code> Plausibility of one edge <code>find_anchors(seeds, topn)</code> <code>list[tuple]</code> Top PPR nodes <code>get_neighbors(node_id)</code> <code>dict</code> A node's neighborhood <code>retrain_model()</code> <code>bool</code> Force NPLL retrain <code>has_npll</code> <code>bool</code> Whether NPLL is active <code>get_status()</code> <code>dict</code> Engine status"},{"location":"reference/result-schema/","title":"Result Schema","text":"<p><code>engine.retrieve(...)</code> returns a single dictionary. This page documents every top-level field.</p> <pre><code>result = engine.retrieve(seeds=[\"entity/claim_123\"])\nresult.keys()\n# dict_keys(['topk_ppr', 'paths', 'evidence_strength', 'community_relevance',\n#            'insight_score', 'aggregates', 'triage', 'ics',\n#            'used_budget', 'trace'])\n</code></pre>"},{"location":"reference/result-schema/#top-level-fields","title":"Top-level fields","text":"Field Type Description <code>topk_ppr</code> <code>list</code> Structurally important nodes (PPR anchors) used to seed exploration <code>paths</code> <code>list[dict]</code> Ranked, scored paths \u2014 the primary output <code>insight_score</code> <code>float</code> Overall retrieval quality, <code>0.0\u20131.0</code> <code>evidence_strength</code> <code>float</code> Strength of supporting evidence, <code>0.0\u20131.0</code> <code>community_relevance</code> <code>float</code> Relevance to the community scope, <code>0.0\u20131.0</code> <code>aggregates</code> <code>dict</code> Motifs, relation shares, and summary statistics <code>triage</code> <code>dict</code> The <code>0\u2013100</code> prioritization score and its components <code>ics</code> <code>dict</code> Decomposition of <code>insight_score</code> <code>used_budget</code> <code>dict</code> Exploration budget consumed <code>trace</code> <code>dict</code> PPR/beam traces, params, and timings for observability"},{"location":"reference/result-schema/#paths","title":"<code>paths</code>","text":"<p>Each path is best-first ranked:</p> <pre><code>{\n    \"nodes\": [\"entity/A\", \"entity/B\", \"entity/C\"],\n    \"edges\": [\n        {\"relation\": \"billed_by\", \"score\": 0.89},\n        ...\n    ],\n    \"score\": 0.94,\n}\n</code></pre> Field Meaning <code>nodes</code> Ordered entity IDs along the path <code>edges</code> The relations connecting the nodes, each with a plausibility <code>score</code> <code>score</code> Combined path score (structural + semantic) <pre><code>for p in result[\"paths\"][:5]:\n    print(f\"[{p['score']:.2f}]\", \" -&gt; \".join(str(n) for n in p[\"nodes\"]))\n</code></pre>"},{"location":"reference/result-schema/#aggregates","title":"<code>aggregates</code>","text":"<pre><code>{\n    \"motifs\":          [{\"pattern\": \"A\u2192B\u2192C\", \"count\": 12}, ...],\n    \"relation_share\":  {\"billed_by\": 0.42, \"has_diagnosis\": 0.31, ...},\n    \"snippet_anchors\": [...],\n    \"summary\": {\n        \"total_paths\": 47,\n        \"unique_motifs\": 8,\n        \"unique_relations\": 5,\n        \"total_edges\": 120,\n        \"provenance\": 0.82,\n        \"recency\": 0.74,\n        \"label_coverage\": 0.91,\n        \"motif_density\": 0.36,\n        \"has_baseline\": false,\n        \"low_support\": false,\n    },\n}\n</code></pre> <p>See Motifs &amp; Aggregation for what each summary field means.</p>"},{"location":"reference/result-schema/#triage","title":"<code>triage</code>","text":"<pre><code>{\n    \"score\": 87,                       # 0\u2013100\n    \"components\": {\n        \"provenance\": 0.82,\n        \"recency\": 0.74,\n        \"surprise\": 0.61,\n        \"motif_density\": 0.36,\n        \"controllability\": 1.0,\n        \"label_coverage\": 0.91,\n        \"penalty\": 0.0,\n        \"low_support\": false,\n    },\n    \"dominant_relation\": {...},\n}\n</code></pre> <p>The scoring formula and guards are documented in Triage &amp; Insight Scoring.</p>"},{"location":"reference/result-schema/#trace","title":"<code>trace</code>","text":"<p>Observability data \u2014 safe to log or drop:</p> <pre><code>{\n    \"ppr\":  {...},                     # PPR trace\n    \"beam\": {...},                     # beam-search trace\n    \"params\": {...},                   # the effective parameters\n    \"timings_ms\": {\"total\": 412, ...}, # per-stage timing\n    \"linker_cfg\": {...},\n}\n</code></pre> <pre><code>result[\"trace\"][\"timings_ms\"][\"total\"]   # end-to-end latency in ms\n</code></pre>"},{"location":"reference/result-schema/#empty-results","title":"Empty results","text":"<p>When there is insufficient support (for example, seeds with no reachable neighbors), Odin returns a well-formed result with empty <code>paths</code>, <code>insight_score</code> of <code>0.0</code>, and <code>summary[\"low_support\"] == True</code>. Always check <code>len(result[\"paths\"])</code> before iterating.</p>"},{"location":"reference/schema/","title":"SchemaInspector API","text":"<p><code>SchemaInspector</code> discovers your ArangoDB structure at runtime. See the Schema Introspection guide for usage patterns.</p> <pre><code>from odin import SchemaInspector, inspect_arango_schema\n</code></pre>"},{"location":"reference/schema/#constructor","title":"Constructor","text":"<pre><code>SchemaInspector(db, max_sample_docs: int = 5)\n</code></pre> Parameter Type Default Description <code>db</code> <code>StandardDatabase</code> \u2014 A connected <code>python-arango</code> database <code>max_sample_docs</code> <code>int</code> <code>5</code> Documents sampled per collection to discover fields"},{"location":"reference/schema/#get_schema_map","title":"<code>get_schema_map</code>","text":"<pre><code>get_schema_map(refresh: bool = False) -&gt; dict\n</code></pre> <p>Returns the full schema map. Cached after the first call; pass <code>refresh=True</code> to rebuild.</p> <pre><code>schema = inspector.get_schema_map()\nschema[\"database_name\"]   # str\nschema[\"collections\"]     # list of document collections\nschema[\"edges\"]           # list of edge collections\n</code></pre> <p>Shape:</p> <pre><code>{\n    \"database_name\": \"my_graph\",\n    \"collections\": [\n        {\"name\": \"ExtractedEntities\", \"type\": \"document\",\n         \"count\": 12345, \"fields\": [\"_key\", \"name\", \"type\", ...]},\n        ...\n    ],\n    \"edges\": [\n        {\"name\": \"ExtractedRelationships\", \"count\": 45678,\n         \"from_collections\": [\"ExtractedEntities\"],\n         \"to_collections\": [\"ExtractedEntities\"],\n         \"fields\": [\"_from\", \"_to\", \"relation\", ...]},\n        ...\n    ],\n}\n</code></pre> <p>System collections (names starting with <code>_</code>) are skipped.</p>"},{"location":"reference/schema/#get_collection_info","title":"<code>get_collection_info</code>","text":"<pre><code>get_collection_info(collection_name: str) -&gt; dict | None\n</code></pre> <p>Returns the schema entry for a document or edge collection, or <code>None</code> if not found.</p> <pre><code>info = inspector.get_collection_info(\"ExtractedEntities\")\ninfo[\"fields\"]    # discovered field names\n</code></pre>"},{"location":"reference/schema/#get_edge_info","title":"<code>get_edge_info</code>","text":"<pre><code>get_edge_info(edge_collection: str) -&gt; dict | None\n</code></pre> <p>Returns the schema entry for an edge collection, including its <code>from_collections</code> and <code>to_collections</code>.</p> <pre><code>edge = inspector.get_edge_info(\"ExtractedRelationships\")\nedge[\"from_collections\"]   # e.g. [\"ExtractedEntities\"]\nedge[\"to_collections\"]     # e.g. [\"ExtractedEntities\"]\n</code></pre>"},{"location":"reference/schema/#inspect_arango_schema","title":"<code>inspect_arango_schema</code>","text":"<pre><code>inspect_arango_schema(db, output_file: str = \"schema.json\") -&gt; dict\n</code></pre> <p>Convenience one-call helper: builds the schema map and optionally writes it to <code>output_file</code> as JSON.</p> <pre><code>from odin import inspect_arango_schema\n\ninspect_arango_schema(db, output_file=\"schema.json\")\n</code></pre> <p>Use the exported file to prime an agent, generate documentation, or validate structures across environments.</p>"},{"location":"reference/schema/#data-classes","title":"Data classes","text":"<p>The inspector returns plain dictionaries, backed internally by these dataclasses:</p> Dataclass Fields <code>CollectionSchema</code> <code>name</code>, <code>type</code>, <code>count</code>, <code>fields</code> <code>EdgeSchema</code> <code>name</code>, <code>count</code>, <code>from_collections</code>, <code>to_collections</code>, <code>fields</code> <code>SchemaMap</code> <code>database_name</code>, <code>collections</code>, <code>edges</code>"},{"location":"reference/troubleshooting/","title":"Troubleshooting","text":"<p>Common issues and how to resolve them.</p>"},{"location":"reference/troubleshooting/#intelligence_mode-is-constant-not-npll","title":"<code>intelligence_mode</code> is <code>Constant</code>, not <code>NPLL</code>","text":"<p><code>engine.get_status()[\"intelligence_mode\"] == \"Constant\"</code> means the NPLL model is not active. Causes:</p> <ul> <li><code>auto_train=False</code> \u2014 you disabled training. Set <code>auto_train=True</code>.</li> <li>Empty or tiny graph \u2014 there were not enough edges to train a model.</li> <li>Training raised \u2014 Odin caught an error and fell back. Check logs under the <code>odin</code> logger.</li> </ul> <p>You still get PPR-driven structural exploration in constant mode; you lose semantic pruning until a model trains. See Model Lifecycle.</p> <pre><code>import logging\nlogging.getLogger(\"odin\").setLevel(logging.INFO)\n</code></pre>"},{"location":"reference/troubleshooting/#first-retrieval-is-very-slow","title":"First retrieval is very slow","text":"<p>The first <code>retrieve()</code> (or engine construction) trains or loads the NPLL model \u2014 2\u20135 minutes to train, ~30 seconds to load. Warm the engine before serving traffic, and reuse a single engine instance rather than constructing one per request. See Production Deployment.</p>"},{"location":"reference/troubleshooting/#retrieve-returns-no-paths","title":"<code>retrieve()</code> returns no paths","text":"<p>An empty <code>paths</code> list with <code>summary[\"low_support\"] == True</code> usually means:</p> <ul> <li>The seed IDs do not exist or are misformatted (use the full <code>collection/key</code> form).</li> <li>The seeds have no reachable neighbors within <code>hop_limit</code>.</li> <li>The scope (<code>community_id</code> / <code>community_mode</code>) excludes the relevant subgraph.</li> </ul> <p>Verify a seed exists and inspect its neighborhood:</p> <pre><code>engine.get_neighbors(\"entity/claim_123\")[\"degree\"]   # should be &gt; 0\n</code></pre>"},{"location":"reference/troubleshooting/#triage-score-is-surprisingly-low","title":"Triage score is surprisingly low","text":"<p>Inspect the components \u2014 a guard may have fired:</p> <pre><code>result[\"triage\"][\"components\"]\n</code></pre> <ul> <li><code>penalty == 15.0</code> \u2192 label coverage below 0.8; improve node/edge labeling.</li> <li><code>low_support == True</code> \u2192 too little evidence; broaden seeds or raise <code>max_paths</code>.</li> <li>Low <code>recency</code>/<code>provenance</code> \u2192 edges lack <code>created_at</code>/<code>provenance</code> metadata.</li> </ul> <p>See Triage &amp; Insight Scoring.</p>"},{"location":"reference/troubleshooting/#connection-errors-to-arangodb","title":"Connection errors to ArangoDB","text":"<ul> <li>Confirm the database is reachable: <code>curl http://localhost:8529/_api/version</code>.</li> <li>Check host, database name, username, and password.</li> <li>In Docker, ensure the port is published (<code>-p 8529:8529</code>).</li> </ul> <p>See Connecting ArangoDB.</p>"},{"location":"reference/troubleshooting/#importerror-wrong-package","title":"<code>ImportError</code> / wrong package","text":"<p>Install the published package and import the short name:</p> <pre><code>pip install odin-engine\n</code></pre> <pre><code>from odin import OdinEngine       # correct\n# not: import odin_engine\n</code></pre>"},{"location":"reference/troubleshooting/#out-of-memory-on-large-graphs","title":"Out-of-memory on large graphs","text":"<p>Memory scales with <code>cache_size</code> and graph density (~500 MB\u20132 GB typical). Lower <code>cache_size</code> to reduce the resident working set at the cost of more database round-trips. See Caching.</p>"},{"location":"reference/troubleshooting/#high-latency","title":"High latency","text":"<p>Inspect where time goes before changing parameters:</p> <pre><code>result[\"trace\"][\"timings_ms\"]\n</code></pre> <p>Then, per Tuning Retrieval: narrow <code>beam_width</code>, reduce <code>hop_limit</code>, or tighten seeds.</p>"},{"location":"reference/troubleshooting/#still-stuck","title":"Still stuck?","text":"<p>Open an issue with a minimal reproduction: github.com/Prescott-Data/Odin-1/issues.</p>"}]}